---
description: 并发设计分析：分析 JVM 中的并发/同步设计（SafePoint、锁、CAS、内存屏障等）。
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:31:22.328Z
provider: 
---

# JVM-Concurrency-Design：并发设计分析

## 适用场景
- 分析 JVM 中的并发/同步机制（SafePoint、Handshake、锁、CAS、屏障）
- 理解多线程场景下的正确性保证
- 分析 JVM 内部的线程协作模型

## 核心原则
**并发问题的本质是：多个线程同时操作共享数据时，如何保证正确性和性能。分析任何并发设计，都要回答"保护什么、防止什么、代价多大"。**

## 执行步骤

### Step 1：识别并发场景
- **哪些线程参与**：Java 线程？GC 线程？编译线程？VM 线程？
- **共享什么数据**：哪些数据被多个线程同时访问？
- **操作类型**：读-读？读-写？写-写？
- **频率**：这个并发访问发生得多频繁？

### Step 2：分析正确性威胁
不加保护会出什么问题：
- **数据竞争（Data Race）**：同时读写，读到不一致的中间状态
- **原子性违反**：复合操作被中断（check-then-act）
- **可见性问题**：一个线程的修改对其他线程不可见
- **有序性问题**：编译器/CPU 重排导致逻辑乱序
- **ABA 问题**：CAS 无法检测到值被改回来了

### Step 3：分析保护机制
JVM 使用的并发原语：

| 机制 | 适用场景 | 代价 |
|------|---------|------|
| **Mutex/Monitor** | 临界区保护，长时间持有 | 线程阻塞、上下文切换 |
| **CAS (Atomic::cmpxchg)** | 无锁更新，短操作 | 忙等、ABA 风险 |
| **内存屏障 (OrderAccess)** | 保证可见性和有序性 | CPU 流水线停顿 |
| **SafePoint** | 全局一致性快照（STW） | 所有 Java 线程暂停 |
| **Handshake** | 单线程级别的操作请求 | 目标线程在 SafePoint 检查时执行 |
| **Thread-local** | 避免共享 | 合并时需要额外操作 |

### Step 4：分析具体实现
对目标机制的实现进行分析：
- 用了什么并发原语
- 锁的粒度（全局锁？分段锁？无锁？）
- 关键代码路径的锁持有时间
- 是否有 fast path 避免加锁

### Step 5：分析性能影响
- **锁争用情况**：多少线程可能同时竞争？
- **等待时间**：争用时线程等多久？自旋还是阻塞？
- **可扩展性**：线程数增加时性能如何变化？
- **与 GC 的交互**：是否可能在持有锁时被 SafePoint 暂停？

### Step 6：输出分析结论
1. **并发场景描述**：哪些线程、共享什么、做什么操作
2. **正确性威胁**：不保护会出什么问题
3. **保护策略**：用了什么机制、保护了什么
4. **源码分析**：关键同步代码 + 注释
5. **性能分析**：代价多大、可扩展性如何
6. **已知问题/改进空间**：有没有潜在的性能瓶颈

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`