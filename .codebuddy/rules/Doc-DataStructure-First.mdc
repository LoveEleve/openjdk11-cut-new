---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-25T09:10:07.258Z
provider: 
---

# Doc-DataStructure-First：程序 = 数据结构 + 算法

## 核心教训（来自 Day 30 方法解析文档的惨痛返工）

**急于输出算法流程、跳过数据结构完整分析 = 必然返工。**

第一版 Day 30 文档的问题：
1. LinkInfo、CallInfo、vtableEntry 等数据结构只是一笔带过，没有完整字段分析
2. 算法描述是"源码翻译"而不是"设计解释"
3. vtable/itable 只讲查找不讲构建，等于只看了一半
4. `Method::_vtable_index` 一个字段两种编码、7 个 caller、状态转换全部没追踪
5. 缺少数据结构关系图

**结果**：整篇文档推翻重写，浪费一倍时间。

---

## 硬性规则（每次写分析文档必须遵守）

### 规则 1：先数据结构，后算法（不可逆转）

写文档的顺序**必须**是：

```
Step 1：穷举数据结构清单
    ↓
Step 2：逐个完整分析每个数据结构
    ↓
Step 3：分析算法流程（引用已分析好的数据结构）
    ↓
Step 4：画数据结构关系图
```

**禁止**在 Step 2 完成之前写 Step 3。

### 规则 2：数据结构分析的完整性标准

对每个涉及的数据结构，**必须**覆盖以下 6 项：

| # | 必须项 | 说明 | 反例（禁止） |
|---|--------|------|-------------|
| 1 | **全部字段** | 列出所有字段，不能省略 | "主要字段有 _name, _signature 等" |
| 2 | **每个字段的含义** | 一句话说清楚是什么、用在哪 | 只列字段名不解释 |
| 3 | **sizeof** | GDB 验证或源码推算 | 不提大小 |
| 4 | **创建位置** | 谁创建的、在哪个函数、什么时机 | 不提来源 |
| 5 | **关键字段的生命周期** | 谁设置 → 何时设置 → 设置什么值 → 谁读取 | 只讲"有这个字段" |
| 6 | **一个字段多种编码时画值域图** | 如 Method::_vtable_index 的 vtable/itable/sentinel 编码 | 只提"可以存两种值" |

### 规则 3：算法描述的质量标准

对每个算法/流程，**必须**按此顺序描述：

```
1. 解决什么问题（为什么需要这个算法）
2. 核心思路（一句话概括）
3. 输入是什么（引用 Step 2 的数据结构）
4. 输出是什么（引用 Step 2 的数据结构）
5. 步骤分解（每步用自然语言 + 源码关键行）
6. 关键设计决策（为什么这样而不是那样）
```

**禁止**的算法描述方式：
- ~~"然后调用了 XXX 函数"~~ → 要说"为了解决 Y 问题，调用 XXX 做了 Z"
- ~~贴 50 行源码然后说"如上所示"~~ → 要先自然语言描述，再贴关键 5-10 行
- ~~只讲查找/使用~~ → 还要讲构建/初始化过程

### 规则 4：关系图是必须项

每篇分析文档的**最后一节之前**，必须有一个 Mermaid 数据结构关系图，展示：
- 所有涉及的数据结构
- 它们之间的包含/引用/继承关系
- 关键字段之间的指向关系

### 规则 5：总结必须分两个维度

文档总结**必须**分为：
- **数据结构层面**：涉及了哪些结构、每个结构的核心特征
- **算法层面**：涉及了哪些算法、每个算法的核心设计决策

---

## 文档模板（强制结构）

```markdown
# 标题

> 纯源码分析，基于 OpenJDK 11 slowdebug
> 方法论：程序 = 数据结构 + 算法

---

## 一、宏观理解
### 1.1 解决什么问题
### 1.2 总体调用链（Mermaid 图）
### 1.3 涉及的数据结构清单（表格）

## 二、数据结构全景 ⭐（必须在算法之前）
### 2.1 结构A（完整 6 项分析）
### 2.2 结构B（完整 6 项分析）
### 2.3 结构C（完整 6 项分析）
...

## 三、算法/流程分析（引用第二节的数据结构）
### 3.1 算法A（按规则 3 的 6 步描述）
### 3.2 算法B
...

## N-1、数据结构关系图（Mermaid）

## N、总结
### N.1 数据结构层面
### N.2 算法层面
```

---

## 自检清单（写完文档后逐项检查）

- [ ] 每个数据结构都有完整的字段列表？（不是"等"、"主要字段"）
- [ ] 每个字段都有含义说明？
- [ ] 关键数据结构有 sizeof？
- [ ] 关键字段有生命周期追踪？（谁设置、何时、什么值）
- [ ] 有多种编码的字段画了值域图？
- [ ] 算法描述先讲"解决什么问题"？
- [ ] 不存在"源码翻译"式描述？（每步都有 why）
- [ ] 涉及的结构既讲了查找/使用也讲了构建/初始化？
- [ ] 有数据结构关系图（Mermaid）？
- [ ] 总结分了数据结构和算法两个维度？