---
description: 教学文档输出：从零讲解一个主题，面向学习。结构：问题引入→概念→源码→图示→总结。
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:33:21.799Z
provider: 
---

# JVM-Doc-Tutorial：教学文档输出

## 适用场景
- 将分析结果整理成可学习的教学文档
- 面向"想要理解这个主题"的读者
- 从零开始讲解一个 JVM 主题

## 核心原则
**教学文档的目标是让读者学会，不是展示作者有多懂。每一段都要服务于读者的理解过程。**

## 文档结构模板

```markdown
# 标题：XXX 深度解析

> 基于 OpenJDK 11 源码分析
> 标准环境：-Xms8g -Xmx8g -XX:+UseG1GC, G1 Region = 4MB
> 源码路径：src/hotspot/share/xxx/

---

## 1. 问题引入

（用一个具体场景引出问题，让读者感受到"确实需要解决"）

### 1.1 场景描述
在 XXX 场景下...

### 1.2 核心问题
如果不解决这个问题，会导致...

### 1.3 问题量化
具体有多严重（数据/对比）

---

## 2. 核心概念

（先建立概念框架，不涉及代码）

### 2.1 设计思路
一句话核心思路 + 展开解释

### 2.2 关键概念
逐个介绍需要了解的概念（每个概念：是什么 + 为什么需要）

### 2.3 整体架构图
（文字描述 + ASCII 图）

---

## 3. 源码实现

（现在才开始看代码）

### 3.1 核心数据结构
每个数据结构：
- 定义位置
- 字段含义
- 内存布局图
- GDB 验证数据

### 3.2 核心流程
每个步骤：
- 做了什么（自然语言）
- 为什么（回溯到第1节的问题）
- 源码（关键片段，不要贴大段）
- GDB 验证

### 3.3 关键细节
（深入几个最重要/最难理解的点）

---

## 4. 实际运行验证

### 4.1 GDB 验证数据
（关键数据的 GDB 输出）

### 4.2 JVM 参数相关
（和这个主题相关的 JVM 参数，加什么参数能看到什么日志）

---

## 5. 总结

### 5.1 核心要点
（3-5 个 bullet point，概括全文）

### 5.2 关联知识
（与其他模块的关系，可以延伸阅读的方向）

### 5.3 常见误解
（对这个主题常见的错误理解，纠正之）
```

## 写作规范

### 源码引用规范
- 不要贴超过 30 行的源码块
- 贴源码时必须标注文件路径和行号
- 关键行要有注释说明
- 省略不相关的代码（用 `// ... 省略 ...` 标注）

### 图示规范
- 内存布局用 ASCII 表格
- 流程用编号列表或简单 ASCII 流程图
- 关系用 ASCII 框图
- 复杂图示建议额外用 drawio（配合 JVM-Doc-Diagram）

### 数据规范
- 所有数值数据必须标注来源（GDB / 源码计算 / 理论值）
- 使用标准条件下的数据
- 涉及大小的用人类可读格式（如 "4MB" 而不是 "4194304"）

### 语言规范
- 用"我们"而不是"你"（教学语气）
- 先结论后展开
- 技术术语首次出现要解释
- 每个小节不超过 500 字

## 文档输出位置
- 主题目录：`/data/workspace/openjdk-cut-new/jvm-md/<主题文件夹>/`
- GDB 数据：同目录下 `gdb_xxx.txt`
- 命名规范：`<编号>_<主题名>.md`

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`