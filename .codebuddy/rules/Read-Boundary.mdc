---
description: 边界探测法：只看接口不看实现，搞清楚输入输出和副作用。适合回答"这个函数我需要了解到什么程度？"
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:28:26.254Z
provider: 
---

# Read-Boundary：边界探测法

## 适用场景
- "这个函数我需要深入看吗？"
- 追踪流程时遇到一个子调用，需要决定是否下钻
- 只需要知道一个组件"做了什么"而不需要知道"怎么做的"
- 建立模块间的接口认知

## 核心原则
**不是所有代码都值得深入。先搞清楚黑盒的输入、输出、前置条件、后置条件和副作用，再决定是否要打开它。**

## 执行步骤

### Step 1：提取函数签名
```
返回值类型 类名::方法名(参数1类型 参数1名, 参数2类型 参数2名, ...) const?
```
从签名中提取信息：
- **返回值**：返回什么？void 意味着通过副作用产生效果
- **参数**：每个参数的含义，哪些是输入、哪些是输出（指针/引用参数）
- **const**：是否修改对象状态
- **TRAPS**：是否可能抛异常

### Step 2：读取文档注释
- 看函数上方的注释（JVM 源码通常有注释说明）
- 看 .hpp 文件中的声明注释（通常比 .cpp 中的更概括）

### Step 3：快速扫描函数体（不深入）
用 30 秒快速扫描：
- 函数长度（行数）→ 感受复杂度
- return 语句 → 确认返回了什么
- 成员变量赋值 → 确认修改了什么状态
- 全局变量/单例访问 → 确认有哪些外部副作用
- 其他对象的方法调用 → 确认影响了哪些外部对象
- **不要试图理解逻辑**

### Step 4：建立黑盒模型
填写以下模板：

```
┌─────────────────────────────────────────┐
│ 函数名：XXX::YYY()                        │
├─────────────────────────────────────────┤
│ 输入：                                    │
│   - 参数A：含义                            │
│   - 参数B：含义                            │
│   - 隐含输入：this->_field（依赖的状态）     │
├─────────────────────────────────────────┤
│ 输出：                                    │
│   - 返回值：含义                           │
│   - 修改 this->_field1 为...              │
│   - 修改 参数B 指向的内容为...              │
├─────────────────────────────────────────┤
│ 副作用：                                   │
│   - 分配了内存（在哪个区域）                 │
│   - 修改了全局状态 XXX                      │
│   - 获取/释放了锁 YYY                      │
├─────────────────────────────────────────┤
│ 前置条件：                                 │
│   - 必须持有锁 ZZZ                         │
│   - 参数A 不能为 NULL                      │
│ 后置条件：                                 │
│   - 返回值一定不为 NULL（或失败时为 NULL）    │
├─────────────────────────────────────────┤
│ 复杂度：XX 行，是否值得深入：是/否            │
│ 深入理由/跳过理由：...                      │
└─────────────────────────────────────────┘
```

### Step 5：做出决策
基于黑盒模型决定：
- **跳过**：黑盒模型已经足够理解上层逻辑 → 记录黑盒模型，继续
- **浅入**：需要知道大致流程但不需要每行代码 → 用 Read-TopDown 的 Step 2 只做第一层展开
- **深入**：这是核心逻辑，必须完全理解 → 切换到 Read-TopDown 或其他方法

### Step 6：输出
- 如果跳过：输出黑盒模型即可
- 如果浅入/深入：黑盒模型作为前置摘要，附在详细分析前面

## 判断是否深入的规则

**必须深入的信号：**
- 这个函数是当前分析主题的核心逻辑
- 返回值/副作用直接影响后续关键流程
- 函数名出现在面试高频题中
- 你无法从签名和注释推断出它做了什么

**可以跳过的信号：**
- 工具函数（align_up、MAX2、log2 等）
- 纯日志/统计函数
- 你已经在其他文档中分析过了
- 与当前分析主题无关的辅助逻辑

## 注意事项
- 这个方法的核心价值是**节省时间**——不是所有代码都值得深入
- 黑盒模型要**准确**，宁可多花 1 分钟确认，不要猜错
- 如果对副作用判断不确定，用 GDB 设断点在函数前后打印状态变化
- 记录所有"跳过"的函数，后续可以按需回来深入

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`