---
description: 参考手册文档输出：某个组件的完整参考手册。结构：概览→字段→内存布局→GDB数据→关联关系。
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:33:43.101Z
provider: 
---

# JVM-Doc-Reference：参考手册文档输出

## 适用场景
- 为一个 JVM 组件生成完整的参考手册
- 面向"需要查阅具体细节"的读者
- 全面、准确、可查阅

## 核心原则
**参考手册不是教程，不需要循序渐进。它的目标是完整、准确、方便查阅。像字典一样，需要什么查什么。**

## 文档结构模板

```markdown
# 标题：XXX 参考手册

> OpenJDK 11 | 标准条件：-Xms8g -Xmx8g -XX:+UseG1GC
> 源码位置：src/hotspot/share/xxx/yyy.hpp

---

## 1. 概览

| 属性 | 值 |
|------|---|
| 类名 | `ClassName` |
| 所属子系统 | xxx |
| 继承链 | A → B → ClassName |
| 源文件 | xxx.hpp / xxx.cpp |
| 大小 | sizeof = XXX bytes |
| 生命周期 | 与进程同/动态创建销毁 |
| 创建位置 | `函数名()` @ 文件:行号 |
| 全局访问方式 | `指针/方法` |

### 1.1 一句话描述
...

### 1.2 在系统中的位置
（与其他组件的关系图）

---

## 2. 字段详解

### 2.1 完整字段列表

| 偏移 | 字段名 | 类型 | 大小 | 含义 | GDB 验证值 |
|------|--------|------|------|------|-----------|
| 0x00 | [vtable] | ptr | 8 | 虚函数表 | 0x... |
| 0x08 | _field1 | Type1 | N | 含义 | 值 |
| ... | ... | ... | ... | ... | ... |

### 2.2 关键字段深入

#### _field1
- **类型**：...
- **含义**：...
- **赋值时机**：在 XXX 阶段被设置
- **使用场景**：被 YYY 和 ZZZ 使用
- **取值范围**：...
- **关联参数**：受 -XX:ParamName 影响

（对每个重要字段重复此格式）

---

## 3. 内存布局

### 3.1 布局图
（ASCII 内存布局图）

### 3.2 子结构展开
（指针字段指向的子结构的布局）

---

## 4. 核心方法

| 方法名 | 职责 | 调用频率 | 关键程度 |
|--------|------|---------|---------|
| method1() | 一句话描述 | 高/中/低 | 核心/辅助 |
| method2() | 一句话描述 | 高/中/低 | 核心/辅助 |

### 4.1 关键方法详解

#### method1()
- **签名**：`返回值 类::方法(参数)`
- **职责**：...
- **调用者**：被谁调用
- **流程摘要**：（编号列表）
- **源码位置**：文件:行号

---

## 5. 关联关系图

（这个组件与外部的所有关系，ASCII 图）

---

## 6. GDB 验证数据

### 6.1 GDB 命令
（复制即可使用的 GDB 命令）

### 6.2 验证输出
（实际 GDB 输出）

---

## 7. 相关 JVM 参数

| 参数 | 默认值 | 影响 |
|------|--------|------|
| -XX:Param1 | value | 影响 _field1 的计算 |

---

## 8. 参考

- 相关文档链接（jvm-md 中的其他文档）
- 相关源码文件列表
```

## 写作规范
- 每个字段都必须有 GDB 验证数据
- 表格优先于段落（方便查阅）
- 不要省略任何字段（参考手册要完整）
- 如果字段是复杂子结构，在"子结构展开"章节单独分析

## 文档输出位置
- `jvm-md/<子系统目录>/<组件名>_reference.md`

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`