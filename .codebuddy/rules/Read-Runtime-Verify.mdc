# Read-Runtime-Verify：实际验证原则

## 核心理念

**任何理论推导必须经过实际验证，不接受未经证实的结论。**

## 适用场景

- 当你完成源码分析后，需要验证结论的正确性
- 当你遇到不确定的行为或参数时，需要通过实际运行确认
- 当你发现异常值时，必须深入分析而非敷衍

## 验证方法论

### 1. 三层验证原则

**Level 1：系统调用验证（strace）**
- 用途：验证系统调用参数、返回值、调用时机
- 方法：`strace -f -e trace=syscall_name program`
- 示例：验证 `perf_event_open` 的参数
  ```bash
  strace -f -e trace=perf_event_open java -agentpath:...
  ```

**Level 2：独立程序验证（C/C++）**
- 用途：隔离验证特定机制，排除复杂环境干扰
- 方法：编写最小可复现程序
- 示例：验证 `siginfo_t` 结构和 SIGPROF 信号
  ```c
  void handler(int sig, siginfo_t *info, void *uc) {
      printf("si_code = %d\n", info->si_code);
  }
  ```

**Level 3：运行时调试验证（GDB）**
- 用途：在真实运行环境中验证函数调用、参数传递、数据结构
- 方法：GDB attach 到运行进程，使用脚本文件设置断点
- 示例：验证 `walkVM` 的调用
  ```gdb
  break StackWalker::walkVM
  commands
    printf "max_depth = %d\n", $rdx
    continue
  end
  ```

### 2. GDB 使用规范

**❌ 错误做法：批处理命令格式不规范**
```bash
# 批处理模式命令格式问题
gdb -batch \
  -ex "set \$var = (type*)\$reg" \
  -ex "printf \"value = %d\n\", \$var->field"
# 问题：set 命令后面不能直接跟 printf
```

**✅ 正确做法：使用脚本文件**
```gdb
# verify.gdb 文件
break FunctionName
commands
  set $var = (type*)$reg
  printf "value = %d\n", $var->field
  continue
end
```

```bash
gdb -batch -x verify.gdb -p PID
```

**关键点**：
- GDB 批处理模式下的命令必须符合语法规范
- 复杂命令使用脚本文件，而非命令行参数
- 使用 `-x script.gdb` 而非 `-ex "command"`

### 3. 异常值处理原则

**遇到异常值时的标准流程**：

```
┌─────────────────────────────────────────────────────┐
│  步骤 1：立即停止，不要继续                           │
│    - 不要用"可能是..."敷衍                           │
│    - 不要假设是工具问题                              │
├─────────────────────────────────────────────────────┤
│  步骤 2：深入分析异常值的来源                         │
│    - 检查输出上下文（时间、线程、调用栈）            │
│    - 检查工具命令格式是否正确                        │
│    - 检查是否打印了正确的变量/字段                   │
├─────────────────────────────────────────────────────┤
│  步骤 3：使用其他方法交叉验证                         │
│    - 至少用两种不同的方法验证                        │
│    - 例如：GDB + C++ 程序，或 strace + GDB          │
├─────────────────────────────────────────────────────┤
│  步骤 4：确认正确值并更新文档                         │
│    - 明确标注错误输出的原因                          │
│    - 给出正确值及其验证方法                          │
│    - 更新相关文档                                    │
└─────────────────────────────────────────────────────┘
```

**案例：max_depth 异常值分析**

错误输出：
```
max_depth = 170882
```

分析过程：
1. **检查上下文**：发现 `LWP 170882`（线程 ID）
2. **检查 GDB 日志**：找到正确的输出 `max_depth=2048`
3. **原因**：printf 命令在线程切换前执行，打印了错误的值
4. **正确值**：`max_depth = 2048`

**案例：si_code 异常值分析**

错误输出：
```
si_code = -2065395852
```

分析过程：
1. **检查 GDB 命令**：批处理格式可能有问题
2. **使用 C++ 程序验证**：编写独立程序直接打印 `info->si_code`
3. **结果**：`si_code = 1`（SI_KERNEL，内核发送）
4. **原因**：GDB 批处理命令格式不正确，导致错误输出

### 4. 多方法交叉验证

**单一验证方法不可靠，必须交叉验证**：

| 方法 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| strace | 系统调用级别，最底层 | 输出量大，难以过滤 | 系统调用参数验证 |
| C++ 程序 | 简单、可靠、可重复 | 需要额外编码 | 机制验证、数据结构验证 |
| GDB | 运行时环境、真实数据 | 需要符号、命令复杂 | 函数调用、参数传递 |

**交叉验证示例**：

```bash
# 方法 1：strace 验证 perf_event_open 参数
strace -f -e trace=perf_event_open java -agentpath:...

# 方法 2：C++ 程序验证 siginfo_t 结构
./test_siginfo

# 方法 3：GDB 验证 walkVM 调用
gdb -x verify.gdb -p JAVA_PID
```

## 验证文件组织

**目录结构**：
```
jvm-md/tmp-file/<主题>/
├── verify_XXX.cpp        # C++ 验证程序
├── verify_XXX.sh         # Shell 验证脚本
├── verify_XXX.gdb        # GDB 脚本
├── strace_XXX.log        # strace 日志
└── verify_result.md      # 验证结果总结
```

**示例**：
```
jvm-md/tmp-file/lesson03/
├── verify_perf_event.cpp       # 验证 perf_event 机制
├── verify_walkvm.sh            # 验证 walkVM 调用
├── attach_verify.gdb           # GDB attach 脚本
├── strace_perf.log             # strace 日志
└── verify_result.md            # 验证结果总结
```

## 验证报告模板

**每个验证必须包含**：

```markdown
## 验证结果

### 验证项
- 目标：验证 XXX
- 方法：strace / C++ 程序 / GDB

### 验证过程
1. 步骤 1：...
2. 步骤 2：...

### 验证数据
```
原始输出：
  value = XXX

处理后的正确值：
  value = YYY
```

### 异常值分析（如果有）
- 异常值：XXX
- 原因：工具使用不当/输出时机错误
- 正确值：YYY
- 验证方法：C++ 程序

### 结论
- ✅ 验证成功：与文档预期一致
- ❌ 验证失败：需要修正文档

### 验证文件
- 源码：verify_XXX.cpp
- 脚本：verify_XXX.sh
- 日志：verify_XXX.log
```

## 关键原则

1. **验证优先**：任何结论必须经过实际验证
2. **多方法验证**：至少用两种不同方法交叉验证
3. **异常必究**：看到异常值必须深入分析，绝不敷衍
4. **文档齐全**：验证过程、结果、文件都要记录

## 常见错误

❌ **错误 1**：只做理论推导，不实际验证
```cpp
// 理论上应该是这样...
// 实际运行可能完全不同！
```

❌ **错误 2**：看到异常值敷衍了事
```
max_depth = 170882  // 可能是 GDB 的问题？不深入分析
```

❌ **错误 3**：只用一种验证方法
```bash
# 只用 GDB，没有交叉验证
gdb -batch -ex "break XXX" ...
```

❌ **错误 4**：GDB 批处理命令格式不规范
```bash
# set 和 printf 在一行，格式错误
gdb -batch -ex "set \$v=1; printf \"%d\", \$v"
```

## 正确做法

✅ **正确 1**：理论推导 + 实际验证
```cpp
// 理论推导：
// 1. 源码分析得出结论 X
// 2. 编写验证程序
// 3. 实际运行确认
// 4. 文档记录
```

✅ **正确 2**：异常值深入分析
```
max_depth = 170882  // 异常！
分析：检查上下文 → 发现 LWP ID
验证：查看完整日志 → 找到正确值 2048
结论：170882 是 LWP ID，正确值是 2048
```

✅ **正确 3**：多方法交叉验证
```bash
# 方法 1：strace
strace -f -e trace=perf_event_open ...

# 方法 2：C++ 程序
./verify_perf_event

# 方法 3：GDB 脚本
gdb -x verify.gdb -p PID
```

✅ **正确 4**：GDB 使用脚本文件
```gdb
# verify.gdb
break FunctionName
commands
  set $var = (type*)$reg
  printf "value = %d\n", $var->field
  continue
end
```

## 验证检查清单

**每次验证前必须确认**：

- [ ] 是否有理论推导？
- [ ] 是否选择了合适的验证方法？
- [ ] 是否准备了验证脚本/程序？
- [ ] GDB 脚本是否使用文件格式？
- [ ] 是否有异常值处理预案？
- [ ] 是否准备了至少两种验证方法？
- [ ] 是否记录了验证过程和结果？
- [ ] 是否更新了相关文档？

## 总结

**验证是分析的生命线，敷衍验证就是敷衍用户。**

**三个绝对原则**：
1. **实际验证 > 理论推导**
2. **异常必究，绝不敷衍**
3. **多方法交叉验证**

**记住**：
- 用户会质疑你的结论
- 用户会验证你的数据
- 用户会检查你的过程
- **你必须经得起考验！**
