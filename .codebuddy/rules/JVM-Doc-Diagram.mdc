---
description: 图示输出：生成 drawio 关系图、内存布局图、流程图，配合文档使用。
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:34:02.845Z
provider: 
---

# JVM-Doc-Diagram：图示输出

## 适用场景
- 需要生成可视化图表辅助理解
- 内存布局图、关系图、流程图
- 配合教学文档或参考手册使用

## 核心原则
**一张好图胜过千言万语。图要清晰、准确、信息密度高。**

## 图表类型

### 1. 内存布局图
用 ASCII 表格在 markdown 文档中直接画：
```
对象布局 (共 XXX 字节)
┌──────────────┬───────────┬──────────┐
│ 偏移          │ 字段       │ 值/含义   │
├──────────────┼───────────┼──────────┤
│ 0x000 (8B)   │ vtable    │ → 虚函数表 │
│ 0x008 (8B)   │ _field1   │ 含义      │
│ 0x010 (4B)   │ _field2   │ 含义      │
│ 0x014 (4B)   │ [padding] │ 对齐      │
└──────────────┴───────────┴──────────┘
```

### 2. 关系图（drawio）
用 drawio XML 格式，输出到 `jvm-md/picture/` 目录：
- 文件命名：`<主题>_关系图.drawio`
- 包含：组件框 + 箭头 + 标注
- 颜色规范：
  - 蓝色：核心组件
  - 绿色：数据结构
  - 橙色：关键路径
  - 灰色：辅助组件

### 3. 流程图（ASCII）
在 markdown 中用 ASCII 画：
```
开始
  │
  ▼
┌─────────┐    Yes   ┌──────────┐
│ 条件判断？ │────────►│ 路径A     │
└────┬─────┘         └────┬─────┘
     │ No                  │
     ▼                     │
┌─────────┐               │
│ 路径B    │               │
└────┬─────┘               │
     │                     │
     ▼                     ▼
┌─────────────────────────────┐
│ 汇合点                       │
└─────────────────────────────┘
```

### 4. 调用链图（ASCII）
```
A::method()
├── B::step1()
│   ├── C::sub1()  ★ 创建了 XXX
│   └── C::sub2()
├── B::step2()
│   └── D::核心操作()  ★ 关键
└── B::step3()
```

## 执行步骤

### Step 1：确定图表类型和内容
- 这张图要展示什么信息
- 读者看这张图要获得什么认知
- 选择合适的图表类型

### Step 2：收集数据
- 从分析结果中提取需要展示的数据
- 确保数据准确（有 GDB 验证）

### Step 3：绘制
- ASCII 图直接写在 markdown 中
- drawio 图写成 XML 文件保存到 `jvm-md/picture/`

### Step 4：验证
- 图中的每个数据都要能在文档/GDB 中找到来源
- 检查箭头方向、标注是否正确

## drawio 文件规范
- 保存位置：`/data/workspace/openjdk-cut-new/jvm-md/picture/`
- 文件名：中文描述 + .drawio
- 画布大小：适合在编辑器中查看

## 注意事项
- ASCII 图适合简单的布局/流程，复杂关系用 drawio
- 图中不要包含过多信息，一张图聚焦一个主题
- 大小/偏移量等数字要标注单位
- 图表要配文字说明，不能只有图

## 源码项目路径
- 图片输出：`/data/workspace/openjdk-cut-new/jvm-md/picture/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`