---
description: 对比阅读法：找两个相似实现进行对比，理解设计选择。适合回答"这两个东西有什么区别？"
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:26:57.315Z
provider: 
---

# Read-Diff：对比阅读法

## 适用场景
- "C1 和 C2 有什么区别？"
- "Young GC 和 Full GC 的流程有什么不同？"
- "偏向锁、轻量级锁、重量级锁的差异是什么？"
- 任何需要对比两个或多个相似概念/实现的场景

## 核心原则
**人类通过对比理解事物。找到相似实现的共同骨架，然后聚焦差异点，每个差异点回答"为什么不同"。**

## 执行步骤

### Step 1：确定对比对象
- 明确要对比的 2-3 个对象（不要超过 3 个，否则太复杂）
- 确认它们确实可比（属于同一类别/解决同一类问题）
- 用一句话描述每个对象的核心定位

### Step 2：提取共同骨架
- 找到对比对象共享的抽象结构：
  - 共同的父类/接口
  - 共同的处理阶段（如都经过"初始化→执行→清理"）
  - 共同的输入/输出
- 画出这个共同骨架，作为对比的坐标系

### Step 3：逐维度对比
建立对比表格，维度包括但不限于：

```markdown
| 维度 | 对象A | 对象B | 为什么不同 |
|------|-------|-------|-----------|
| 触发条件 | ... | ... | ... |
| 执行阶段 | ... | ... | ... |
| 涉及的线程 | ... | ... | ... |
| 核心数据结构 | ... | ... | ... |
| 时间复杂度 | ... | ... | ... |
| STW 时间 | ... | ... | ... |
| 适用场景 | ... | ... | ... |
```

**关键：每个差异都要填"为什么不同"这一列。**

### Step 4：找到设计权衡
- 每个差异点背后都有一个设计权衡（trade-off）
- 常见的权衡维度：
  - 吞吐量 vs 延迟
  - 内存开销 vs CPU 开销
  - 简单性 vs 通用性
  - 启动速度 vs 峰值性能
  - 精确性 vs 性能
- 用一段话总结：A 选择了什么、牺牲了什么；B 选择了什么、牺牲了什么

### Step 5：源码级验证差异
- 对每个关键差异点，找到源码中的具体体现
- 贴出对比代码片段（精简到只展示差异部分）
- 如果有条件编译/运行时分支区分二者，标注出来

### Step 6：输出对比分析
输出格式要求：
1. **一句话对比**："A 是...，B 是...，核心区别在于..."
2. **共同骨架**：它们的共同点/共同流程
3. **对比表格**：多维度对比（如 Step 3）
4. **设计权衡总结**：为什么要有两种实现
5. **选择指南**：什么场景该选 A，什么场景该选 B
6. **源码证据**：关键差异的代码对比

## 注意事项
- 对比之前必须先单独理解每个对象（可以先用 Read-TopDown 或 Read-BottomUp）
- 不要为了对比而强行找差异，有些维度可能是相同的，标注"相同"即可
- 对比维度要有意义，不要对比无关紧要的细节
- 如果对比对象差异极大（如 Interpreter vs C2），可能不适合用这个方法，考虑分别独立分析

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`