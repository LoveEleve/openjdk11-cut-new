---
description: 演进溯源法：看一个机制在不同版本/方案中的演进，理解设计选择。适合回答"为什么设计成这样而不是那样？"
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:29:23.790Z
provider: 
---

# Read-History：演进溯源法

## 适用场景
- "为什么 G1 要用 Region 而 CMS 不用？"
- "偏向锁为什么在 JDK 15 被废弃了？"
- "这个设计为什么是这样的，而不是那样的？"
- 理解一个设计决策背后的历史原因和演进逻辑

## 核心原则
**任何设计都不是凭空出现的，它是对前一个方案的改进。理解演进链，就理解了设计的深层原因。**

## 执行步骤

### Step 1：确定当前方案
- 一句话描述当前方案是什么
- 确认是在 OpenJDK 11 中的实现
- 列出当前方案的核心特征

### Step 2：追溯前身方案
找到当前方案的前身：
- 如果是 GC：Serial → Parallel → CMS → G1 → ZGC/Shenandoah
- 如果是锁：无锁 → 偏向锁 → 轻量级锁 → 重量级锁（这是运行时升级链，也是历史引入顺序）
- 如果是编译：解释执行 → C1 → C2 → 分层编译
- 如果是内存：PermGen → Metaspace
- 对于每个前身方案，记录：它的核心思路、它的问题/不足

### Step 3：构建演进链
```
方案V1（时代/版本）
  核心思路：...
  解决了什么问题：...
  遗留什么问题：... ←── 这就是 V2 要解决的
      │
      ▼
方案V2（时代/版本）
  核心改进：针对 V1 的问题，做了...
  解决了什么问题：...
  遗留什么问题：... ←── 这就是 V3 要解决的
      │
      ▼
方案V3（当前，OpenJDK 11）
  核心改进：针对 V2 的问题，做了...
  当前状态：...
  已知限制：...
```

### Step 4：分析每次演进的关键转变
对每次 V(n) → V(n+1) 的转变：
- **驱动力**：什么问题驱动了这次改变？（性能瓶颈？硬件变化？使用场景变化？）
- **核心变化**：改了什么？（数据结构？算法？策略？）
- **权衡转移**：原来的方案在 A 和 B 之间选了 A，新方案为什么选了 B？
- **兼容性**：如何处理旧行为的兼容？

### Step 5：在源码中找到演进痕迹
JVM 源码中经常有历史痕迹：
- 注释中提到 "was previously" / "replaced by" / "deprecated"
- 保留的旧代码路径（通过 flag 开关）
- TODO/FIXME 注释暗示未来改进方向
- 命名中的历史痕迹（如 PermGen 相关的遗留命名）

### Step 6：输出分析结论
1. **演进时间线**：图示化的演进链
2. **每代方案要点**：核心思路 + 优点 + 不足
3. **关键转变分析**：每次演进的驱动力和核心变化
4. **当前方案的定位**：它解决了什么、还有什么限制
5. **未来方向**：从 JDK 12+ 的改进可以看出什么趋势（如果相关）
6. **源码中的历史痕迹**：具体的代码/注释引用

## 注意事项
- 本项目源码是 OpenJDK 11，分析时以此为"当前版本"
- 不需要每次都追溯到最早版本，追溯到能解释当前设计的程度即可
- 历史信息可以结合 JEP（JDK Enhancement Proposal）和已有知识
- 但最终结论要**落回到本地源码**，不能只停留在理论层面
- 演进分析特别适合和 Read-WhyNot 配合使用

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`