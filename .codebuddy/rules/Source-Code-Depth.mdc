---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-25T12:40:06.557Z
provider: 
---

# Source-Code-Depth：算法描述必须源码级深度

## 核心教训（Day 34 同步机制文档返工）

用伪代码描述算法 = 没有深度 = 返工。

## 硬性要求

### 1. 算法描述禁止伪代码，必须使用真实源码

❌ 禁止：
```
function wait(): // 保存 recursions, exit, park, 重入
```

✅ 必须：
```cpp
// objectMonitor.cpp:1480
intptr_t save = _recursions;  // ★ 保存递归计数
_waiters++;
_recursions = 0;              // ★ 清零（完全释放锁）
exit(true, Self);             // ★ 释放锁
```

### 2. 每个函数 4 要素

1. **源码文件:行号**（如 `objectMonitor.cpp:1416-1641`）
2. **解决什么问题**（一句话）
3. **真实源码 + 逐行注释**
4. **设计决策**（为什么这样做）

### 3. 源码裁剪标准

**保留**：核心逻辑、CAS/atomic、状态转换、内存屏障
**省略**：JVMTI/DTRACE/PerfData/纯 assert（用 `// ... 省略` 标注）
**禁止省略**：竞态处理、内存屏障、错误分支逻辑

### 4. 长函数分段

超 100 行函数：先给阶段划分表，再逐阶段贴源码+注释。

### 5. 多策略时必须画对比表

### 6. 深度标准

| 层级 | 是否达标 |
|------|---------|
| L1 自然语言概括 | ❌ |
| L2 伪代码 | ❌ |
| L3 真实源码+少量注释 | ⚠️ |
| **L4 真实源码+逐行注释+设计解释** | **✅ 标准** |
| L5 L4+对比表+状态机图 | ✅✅ |

目标：所有算法描述达到 L4+。