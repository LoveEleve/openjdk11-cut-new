---
description: 自底向上阅读法：从一个具体的类/结构体出发，反向推出它在整个系统中的角色。适合回答"这个类是干嘛的？"
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:26:11.694Z
provider: 
---

# Read-BottomUp：自底向上阅读法

## 适用场景
- "这个类/结构体是干嘛的？"
- "这个字段为什么存在？"
- 遇到一个陌生的类型，需要搞清楚它在系统中的角色

## 核心原则
**不急着看内部实现，先搞清楚外部关系：谁创建它、谁用它、谁销毁它。从使用者的角度反推职责。**

## 执行步骤

### Step 1：基本信息采集
- 找到类/结构体的定义文件（.hpp）
- 记录：
  - 文件位置（属于哪个子系统）
  - 继承关系（父类是谁）
  - 类注释（如果有，通常在类定义上方）
  - 友元类（friend class，暗示了紧密协作关系）

### Step 2：搜索"谁创建了它"
- 搜索 `new ClassName`、`ClassName::create`、`ClassName(` 构造函数调用
- 确定：
  - 在哪个流程中被创建
  - 创建时传入了什么参数
  - 创建后保存到了哪里（局部变量？某个全局指针？某个容器？）
- 如果是全局唯一实例，找到那个全局指针

### Step 3：搜索"谁使用了它"
- 搜索类型名出现的位置（作为参数类型、局部变量类型、成员变量类型）
- 重点关注：
  - 哪些核心函数以它作为参数 → 这些函数就是它的"消费者"
  - 哪些类持有它的指针/引用 → 这些类和它有"拥有"或"协作"关系
  - 它的 public 方法被谁调用 → 这是它提供的"服务"

### Step 4：搜索"谁销毁了它"
- 搜索 `delete`、`~ClassName`、`destroy`、`free` 相关
- 确定生命周期：
  - 与进程同生命周期？（全局单例）
  - 与某个父对象同生命周期？（成员变量）
  - 动态创建销毁？（池化/缓存）

### Step 5：推断角色和职责
基于 Step 2-4 收集的外部关系，用一段话总结：
- 这个类**是什么**（一句话定义）
- **为什么需要它**（不存在它的话，谁会有问题）
- 在系统中处于**什么位置**（画出它与周围组件的关系）
- **生命周期**如何

### Step 6：按需查看内部实现
- 现在才开始看类的成员变量和方法
- 带着 Step 5 的理解去看，每个字段都要能回答"这个字段服务于什么场景"
- 如果某个字段对不上 Step 5 的理解，说明还有遗漏的使用场景，回到 Step 3 补充

### Step 7：输出分析结论
输出格式要求：
1. **一句话定义**：这个类是什么
2. **存在的原因**：为什么需要它，解决什么问题
3. **生命周期**：在哪创建、保存在哪、何时销毁
4. **外部关系图**：与哪些类交互，交互方式
5. **核心字段列表**：每个字段的含义和作用场景
6. **核心方法列表**：关键方法的职责（一句话）
7. **在标准条件下的实际数据**：GDB 验证的真实值（如适用）

## 注意事项
- **绝不从内部开始**，一定要先从外部关系推断角色
- 搜索使用者时要有耐心，可能分布在很多文件中
- 如果发现使用场景超过 3 个，按重要性排序，优先分析最核心的
- 善用 `search_content` 搜索类型名、方法名

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`