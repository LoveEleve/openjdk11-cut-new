---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-27T02:34:22.031Z
provider: 
---

# JVM-Mechanism-Deep-Dive：机制深度分析文档规范

## 适用场景
- 分析某个 JVM 机制/模块的完整实现
- 输出可直接用于面试/教学的深度分析文档
- 典型场景：libjsig.so 信号链、libattach.so Attach 机制、类加载、GC 等

## 核心原则

**原理讲解不凑字数，源码分析要有深度。**

- 第 0 部分：用最少的字把原理讲清楚（本质→为什么→怎么解决→为什么这样）
- 第 1-3 部分：按 skills/rules 规范深入分析源码（数据结构+算法+验证）

---

## 完整文档结构模板

```markdown
# <机制名称> 深度解析

> 基于 OpenJDK 11 源码分析
> 标准环境：-Xms8g -Xmx8g -XX:+UseG1GC

---

## 第 0 部分：核心原理 ⭐

（用最精炼的语言讲清楚原理，不要堆砌场景/表格/代码）

### 0.1 本质是什么？

一句话概括这个机制解决的核心问题。

例如：
- libjsig.so：Unix 信号处理器不能链式调用
- libattach.so：JVM 运行时注入诊断工具
- G1 GC：_region-based incremental parallel compacting GC_

### 0.2 为什么需要？

从根本机制缺陷讲起，解释为什么会有这个问题。

例如：
- libjsig.so：Unix 的 signal() 系统调用每个信号只能注册一个处理器，后者覆盖前者
- libattach.so：诊断工具需要访问运行中 JVM 的内部状态，但 JVM 启动时不可能预装所有工具

### 0.3 怎么解决？

核心思路 + 关键设计（2-3 段话即可）。

例如 libjsig.so：
- 核心思路：拦截 + 委托
- 关键设计：LD_PRELOAD 拦截信号注册函数，用 bitmap 记录哪些信号被 JVM 占用

### 0.4 为什么这样设计？

解释关键设计决策的理由（不是"做了什么"，而是"为什么这样做"）。

例如 libjsig.so：
- 为什么用 bitmap 而不是链表？信号数量固定且少（64/128），bitmap O(1) 查找更快
- 为什么 JVM 处理器优先？JVM 内部信号处理（如 OutOfMemoryError）必须执行，不能被用户代码覆盖
- 为什么用 LD_PRELOAD？必须在 libc 之前拦截信号注册函数

---

## 第 1 部分：数据结构全景

> 遵循 `Doc-DataStructure-First` 规则：先穷举所有涉及的数据结构，再逐个完整分析

### 1.1 数据结构清单

| 结构名 | 源码位置 | 核心作用 |
|--------|----------|----------|
| XXX | xxx.cpp:123 | ... |

### 1.2 结构 A 详细分析

（每个数据结构必须覆盖 6 项：全部字段 + 含义 + sizeof + 创建位置 + 关键字段生命周期 + 值域图）

#### 1.2.1 字段列表
```cpp
struct XXX {
    int _field1;    // 含义
    void* _field2;  // 含义
    // ...
};
```

#### 1.2.2 sizeof 与内存布局
- sizeof(XXX) = ? 字节（GDB 验证：`p sizeof(XXX)`）
- 内存布局图（ASCII 或 Mermaid）

#### 1.2.3 创建位置
- 在哪个函数创建？
- 在哪个时机创建？

#### 1.2.4 关键字段生命周期
- `_field1`：谁设置 → 何时设置 → 设置什么值 → 谁读取

#### 1.2.5 值域图（如适用）
- 一个字段有多种编码时，画值域图

### 1.3 结构 B 详细分析
（同上）

---

## 第 2 部分：算法/流程分析

> 遵循 `Source-Code-Depth` 规则：禁止伪代码，必须真实源码 + 逐行注释 + 设计解释

### 2.1 核心流程概览

（Mermaid 流程图）

### 2.2 流程 A 详细分析

#### 2.2.1 解决什么问题？
（一句话概括）

#### 2.2.2 函数签名与位置
```cpp
// xxx.cpp:1416-1641
void XXX::core_function(args...) {
```

#### 2.2.3 真实源码 + 逐行注释
```cpp
// xxx.cpp:1420
intptr_t save = _recursions;  // ★ 保存递归计数
_waiters++;                   // ★ 增加等待者计数
// ...
```

#### 2.2.4 设计决策
- 为什么这样设计？解决什么问题？
- 有什么边界情况需要处理？

### 2.3 流程 B 详细分析
（同上）

---

## 第 3 部分：GDB 验证

> 遵循 `Read-Runtime-Verify` 规则：所有结论必须实际验证

### 3.1 验证计划
- 验证目标 1：数据结构 sizeof/offset
- 验证目标 2：算法流程走向
- 验证目标 3：关键字段值

### 3.2 GDB 脚本
（保存到 `tmp-file/<主题>/verify.gdb`）

### 3.3 验证结果
（关键 GDB 输出 + 解释）

---

## 第 4 部分：总结

### 4.1 数据结构层面
- 涉及了哪些结构？
- 每个结构的核心特征？

### 4.2 算法层面
- 涉及了哪些算法？
- 每个算法的核心设计决策？

### 4.3 核心要点
- 3-5 个 bullet point，概括全文
```

---

## 写作规范

### 第 0 部分规范（核心原理）

| 要点 | 要求 | 反例（禁止） |
|------|------|-------------|
| 本质 | 一句话概括核心问题 | 模糊描述 |
| 为什么需要 | 从根本机制缺陷讲起 | 罗列场景 |
| 怎么解决 | 核心思路 + 关键设计（2-3 段话） | 堆砌代码 |
| 为什么这样设计 | 解释设计决策的理由 | 只说"做了什么" |
| 长度 | 精炼，不凑字数 | 堆砌表格/场景 |

### 第 1 部分规范（数据结构）

- 遵循 `Doc-DataStructure-First` 规则
- 每个结构必须覆盖 6 项
- 先穷举清单，再逐个分析

### 第 2 部分规范（算法）

- 遵循 `Source-Code-Depth` 规则
- 禁止伪代码
- 每个函数 4 要素：文件:行号 + 解决什么问题 + 真实源码+逐行注释 + 设计决策

### 第 3 部分规范（验证）

- 遵循 `Read-Runtime-Verify` 规则
- 使用 GDB 脚本文件
- 关键结论至少两种方法交叉验证

---

## 与其他 Rules/Skills 的关系

| 规则/Skill | 本规范中的位置 |
|------------|----------------|
| `Doc-DataStructure-First` | 第 1 部分：数据结构全景 |
| `Source-Code-Depth` | 第 2 部分：算法/流程分析 |
| `Read-Runtime-Verify` | 第 3 部分：GDB 验证 |
| `JVM-Object-Layout` | 第 1 部分 sizeof/内存布局 |
| `JVM-GDB-Script` | 第 3 部分 GDB 脚本编写 |
| `JVM-Doc-Diagram` | 全文图表绘制 |

---

## 文档输出位置
- 主文档：`new-jvm-md/<模块>/<主题>-Deep-Dive.md`
- GDB 文件：`new-jvm-md/tmp-file/<主题>/`
- 图表：`new-jvm-md/picture/`