---
description: 自顶向下阅读法：从入口函数开始，逐层展开调用链，先骨架后细节。适合回答"这个功能是怎么实现的？"
alwaysApply: false
enabled: true
updatedAt: 2026-02-07T03:25:49.327Z
provider: 
---

# Read-TopDown：自顶向下阅读法

## 适用场景
- "这个功能是怎么实现的？"
- "从 X 入口到最终效果，中间经历了什么？"
- 需要理解一个完整功能的执行链路

## 核心原则
**先骨架后细节，先主干后分支，逐层下钻但有节制。**

## 执行步骤

### Step 1：确定入口点
- 明确要追踪的入口函数/方法
- 确认调用条件（什么时候、被谁调用）
- 记录入口函数的签名、参数含义

### Step 2：第一层展开（骨架）
- 只看入口函数体内的**直接调用**，不递归进去
- 用伪代码或编号列表列出主干步骤，例如：
  ```
  入口函数 A() {
    1. B()  → 做了什么（一句话）
    2. C()  → 做了什么（一句话）
    3. if 条件 { D() } else { E() }
    4. F()  → 做了什么（一句话）
  }
  ```
- 标注哪些步骤是关键步骤（★），哪些是辅助/检查步骤
- **此阶段禁止深入任何子函数的实现**

### Step 3：确定下钻优先级
- 对第一层的每个调用评估：
  - **必须下钻**：这是核心逻辑，不看就无法理解整体功能
  - **可选下钻**：辅助逻辑，暂时当黑盒，后续按需展开
  - **跳过**：日志、断言、调试代码、错误处理（第一遍忽略）

### Step 4：逐层下钻（递归展开）
- 按优先级依次展开每个"必须下钻"的子函数
- 每个子函数重复 Step 2-3 的过程
- **控制深度**：一般不超过 4-5 层，超过时停下来总结当前认知
- 每展开一层，回头更新上层的理解

### Step 5：绘制调用链全景图
- 用缩进或树形结构画出完整调用链：
  ```
  A()
  ├── B()
  │   ├── B1()  ← 关键：创建了 XXX 对象
  │   └── B2()  ← 注册到全局 YYY
  ├── C()       ← 条件判断，我们的条件走这个分支
  ├── D()
  │   ├── D1()
  │   └── D2()  ← 关键：这里触发了 ZZZ
  └── F()       ← 收尾工作
  ```

### Step 6：输出分析结论
输出格式要求：
1. **一句话总结**：这个入口函数整体做了什么
2. **主干流程**：编号列表，每步一句话说清楚做了什么、为什么
3. **关键节点深入**：对标★的步骤展开详细分析
4. **创建了什么**：列出所有新创建的对象/数据结构，说明保存到哪里
5. **调用链全景图**：如 Step 5
6. **遗留问题**：哪些子函数还没展开，标记待分析

## 注意事项
- **不要一上来就贴大段源码**，先用自己的话描述骨架
- **遇到宏/模板先跳过**，记个标记后续用 Read-Layered 处理
- **遇到条件分支时**，先明确在当前标准条件（-Xms8g -Xmx8g G1GC）下走哪个分支
- **每一层都要基于源码**，不能靠猜，用 search_content / read_file 确认
- 如果静态阅读无法确定分支走向，切换到 Read-Runtime-First（GDB 验证）

## 源码项目路径
- HotSpot 源码根目录：`/data/workspace/openjdk-cut-new/src/hotspot/share/`
- 平台相关代码：`/data/workspace/openjdk-cut-new/src/hotspot/os/linux/` 和 `/data/workspace/openjdk-cut-new/src/hotspot/cpu/x86/`
- 已有分析文档：`/data/workspace/openjdk-cut-new/jvm-md/`