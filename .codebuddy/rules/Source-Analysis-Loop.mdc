---
description: 源码分析持续工作模式 - 选定一个目标，持续迭代直到完全搞懂
alwaysApply: true
enabled: true
updatedAt: 2026-02-14
provider: 
---

# Source-Analysis-Loop：源码分析持续工作模式

## 两种工作模式

### 模式 A：迭代积累（短任务）

每次聚焦一个小问题，1-2 小时完成，适合快速学习零散知识点。

### 模式 B：持续深挖（长任务）⭐ 重点

**选定一个目标，持续分析、反复验证、直到完全搞懂。**

---

## 模式 B：持续深挖详解

### 适用场景

- 用户指定一个具体方法/模块，要求分析透彻
- 例如：`Threads::create_vm()`、`G1CollectedHeap::initialize()`
- 一个晚上或更长时间，持续工作不停止

### 核心原则

1. **选定目标后不要换** - 一直分析到完全搞懂
2. **反复验证** - 同一结论用多种方法验证
3. **不断加深** - 每次迭代比上次更深入
4. **不放过任何细节** - 每个疑问都要解决

### 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    持续深挖工作流                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 锁定目标                                               │
│     ↓                                                       │
│  2. 第一轮：宏观理解（入口 → 核心调用 → 阶段划分）            │
│     ↓                                                       │
│  3. 第二轮：逐段分析（每个阶段的核心逻辑）                    │
│     ↓                                                       │
│  4. 第三轮：深入细节（关键函数 + 数据结构）                    │
│     ↓                                                       │
│  5. 第四轮：GDB 验证（实际运行验证关键结论）                  │
│     ↓                                                       │
│  6. 第五轮：查漏补缺（还有什么没看懂？）                      │
│     ↓                                                       │
│  7. 产出完整文档                                           │
│     ↓                                                       │
│  8. 更新进度                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 每轮执行标准

#### 第一轮：宏观理解

- 找到入口函数
- 画出整体调用骨架
- 划分主要阶段
- **穷举本次涉及的所有数据结构，列出清单**
- 输出：Mermaid 流程图（阶段划分）+ 数据结构清单

#### 第二轮：数据结构全景 ⭐（必须在算法之前完成）

> **遵循 `Doc-DataStructure-First` 规则**

- **逐个**完整分析每个数据结构（不允许跳过）
- 每个数据结构必须覆盖 6 项：全部字段 + 含义 + sizeof + 创建位置 + 关键字段生命周期 + 值域图（如适用）
- 一个字段有多种编码时，画值域图
- **此轮完成前，禁止进入第三轮**
- 输出：完整数据结构分析

#### 第三轮：算法/流程分析

- 每个算法先讲"解决什么问题"再讲"怎么实现"
- 每步引用第二轮已分析好的数据结构
- 不能只讲查找/使用，还要讲构建/初始化
- 禁止"源码翻译"式描述，每步必须有 why
- 输出：完整算法分析

#### 第四轮：GDB 验证

- 设计 GDB 验证计划
- 实际运行验证关键结论（数据结构 sizeof/offset + 算法流程走向）
- 记录验证数据
- 输出：GDB 验证结果

#### 第五轮：查漏补缺 + 关系图

- 回顾所有疑问
- 确认每个疑问都已解决
- 识别可能的错误
- **画数据结构关系图（Mermaid），串联全局**
- 输出：问题清单 + 解答 + 关系图

### 产出要求

#### 文档结构

```
new-jvm-md/<模块>/<目标>-Complete.md

示例：
new-jvm-md/Thread/create_vm-Complete.md
new-jvm-md/G1GC/YoungGC-Complete.md
```

```markdown
# <目标> 完整分析

> 方法论：程序 = 数据结构 + 算法

## 一、宏观理解

### 1.1 解决什么问题
### 1.2 入口与调用链（附 Mermaid 图）
### 1.3 涉及的数据结构清单（表格）

## 二、数据结构全景 ⭐

### 2.1 结构A（全部字段 + sizeof + 生命周期）
### 2.2 结构B
...

## 三、算法/流程分析（引用第二节数据结构）

### 3.1 算法A（先讲解决什么问题 → 再讲实现）
### 3.2 算法B
...

## 四、GDB 验证

### 4.1 验证计划
### 4.2 验证结果

## 五、查漏补缺

### 5.1 问题清单
### 5.2 解答

## 六、数据结构关系图（Mermaid）

## 七、总结
### 7.1 数据结构层面
### 7.2 算法层面
```

#### Mermaid 图要求

每篇文档必须包含：
- 整体流程图（第一轮产出）
- 阶段划分图（第一轮产出）
- 数据结构关系图（第三轮产出）

### 进度更新

每完成一轮，更新 `jvm-md/progress_comprehensive.md`：

```
## <目标> 分析进度

- [x] 第一轮：宏观理解
- [ ] 第二轮：逐段分析
- [ ] 第三轮：深入细节
- [ ] 第四轮：GDB 验证
- [ ] 第五轮：查漏补缺
```

---

## 模式 A：迭代积累（短任务）

### 适用场景

- 快速学习零散知识点
- 每次只有 1-2 小时
- 不是用户指定的核心目标

### 执行步骤

#### Step 1：确定具体问题

| 来源 | 示例 |
|------|------|
| 当前正在看的代码 | "这段代码是做什么的？" |
| 已有文档的未完成部分 | "Phase 3 还没分析" |
| 用户提问 | "对象是怎么分配的？" |

#### Step 2：选择 Skills 组合（2-3 个）

| 问题类型 | Skills 组合 |
|----------|-------------|
| 流程/实现 | `Read-TopDown` + `JVM-Doc-Diagram` |
| 数据结构 | `JVM-Object-Layout` + `JVM-GDB-Memory` |
| 差异/对比 | `Read-Diff` + `JVM-Doc-Diagram` |
| 为什么存在 | `Read-WhyNot` + `JVM-Problem-Driven` |
| 值怎么算 | `Read-DataFlow` + `Read-Runtime-First` |

#### Step 3：执行分析 + 产出文档

```
jvm-md/<模块>/<问题>-<日期>.md
```

#### Step 4：更新进度

---

## 自我约束规则（两种模式通用）

### 必须遵守

1. **产出优先** - 没有产出 = 没有分析
2. **Mermaid 必须** - 每篇文档至少一个 Mermaid 图
3. **GDB 验证** - 关键结论必须实际验证

### 禁止

1. **禁止只读不写** - 必须有文档产出
2. **禁止没有图的文档** - Mermaid 是必须项
3. **禁止验证不彻底** - 关键结论必须验证

---

## 源码项目路径

- JVM 源码：`/data/workspace/openjdk-cut-new/src/hotspot/`
- async-profiler：`/data/workspace/async-profiler/src/`
- Arthas：`/data/workspace/arthas-4.1.2/arthas/core/src/main/java/`
- **文档输出**：`/data/workspace/openjdk-cut-new/new-jvm-md/<模块>/`
- GDB 临时文件：`/data/workspace/openjdk-cut-new/new-jvm-md/tmp-file/`
- 进度更新：`/data/workspace/openjdk-cut-new/new-jvm-md/progress.md`

---

## 标准运行环境

```
JVM：/data/workspace/openjdk-cut-new/build/linux-x86_64-normal-server-slowdebug/jdk/bin/java
参数：-Xms8g -Xmx8g -XX:+UseG1GC -Xint -cp /data/workspace/demo/src com.wjcoder.Main
```
