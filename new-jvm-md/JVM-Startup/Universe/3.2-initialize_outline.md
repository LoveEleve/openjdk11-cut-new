# 3.2 G1CollectedHeap::initialize() 详细子大纲

> 这是 3.2 的递归细化大纲，**真正分配堆内存的地方**
> 标记说明：[ ] 待分析  [~] 部分分析  [x] 已完成

---

## 第 0 部分：核心原理 ⭐

### 0.1 本质是什么？

本文分析 **3.2 G1CollectedHeap::initialize() 详细子大纲** 的初始化过程：JVM 启动时该组件如何被创建、配置和激活，以及初始化顺序的约束关系。

### 0.2 为什么需要？

JVM 初始化顺序有严格的依赖关系——某些组件必须在其他组件之前初始化。理解初始化顺序有助于排查启动失败问题，也能理解各组件的设计约束。

### 0.3 怎么解决？

追踪初始化函数的调用链，分析每个初始化步骤的前置条件和后置效果，识别关键的初始化顺序约束。

### 0.4 为什么这样设计？

初始化顺序的设计原则：「被依赖的先初始化」。例如内存管理必须在 GC 之前初始化，GC 必须在类加载之前初始化。

---


## 整体调用链

```
Universe::initialize_heap()
└── _collectedHeap->initialize()    [g1CollectedHeap.cpp:1587]
    │
    ├── A. 预留虚拟内存
    │   └── Universe::reserve_heap()  → mmap() PROT_NONE
    │
    ├── B. 创建 6 个 G1RegionToSpaceMapper
    │   ├── heap_storage         (8GB 堆本身)
    │   ├── bot_storage          (16MB BOT)
    │   ├── cardtable_storage    (16MB 卡表)
    │   ├── card_counts_storage  (16MB 热卡计数)
    │   ├── prev_bitmap_storage  (128MB 位图)
    │   └── next_bitmap_storage  (128MB 位图)
    │
    ├── C. 初始化核心数据结构
    │   ├── HeapRegionManager._hrm.initialize()
    │   ├── G1CardTable._card_table.initialize()
    │   ├── G1HotCardCache._hot_card_cache.initialize()
    │   ├── G1RemSet._g1_rem_set.initialize()
    │   └── G1BlockOffsetTable._bot 创建
    │
    ├── D. 并发组件创建
    │   ├── G1ConcurrentMark._cm 创建
    │   └── G1ConcurrentRefine._cr 创建
    │
    ├── E. 扩展堆（实际提交内存）
    │   └── expand(init_byte_size)  → mmap() PROT_READ|WRITE
    │
    └── F. 运行时组件初始化
        ├── g1_policy()->init()
        ├── SATBMarkQueueSet 初始化
        ├── DirtyCardQueueSet 初始化
        ├── G1MonitoringSupport 创建
        └── G1StringDedup 初始化
```

---

## A. 预留虚拟内存 ✅ 已完成

> 详见 [A-ReserveHeap.md](A-ReserveHeap.md)

### A.1 Universe::reserve_heap() ✅
```cpp
ReservedSpace heap_rs = Universe::reserve_heap(max_byte_size, heap_alignment);
```
- [x] **A.1.1** mmap() 两阶段内存分配
  - 阶段 1（Reserve）：`PROT_NONE`，只占虚拟地址，不分配物理内存
  - 阶段 2（Commit）：`PROT_READ|WRITE`，实际分配物理页
- [x] **A.1.2** ReservedHeapSpace 构造过程
  - 压缩指针优化的地址选择（Unscaled → ZeroBased → HeapBased）
  - 从高地址向低地址搜索最优位置
- [x] **A.1.3** 三种压缩指针模式
  - Unscaled (≤4GB): 直接截断，零开销
  - ZeroBased (≤32GB): 右移3位
  - HeapBased: 基址+移位

### A.2 initialize_reserved_region() ✅
```cpp
initialize_reserved_region((HeapWord*)heap_rs.base(), 
                           (HeapWord*)(heap_rs.base() + heap_rs.size()));
```
- [x] **A.2.1** 保存堆范围到 `_reserved` (MemRegion)
- [x] **A.2.2** 三个内存描述类的关系
  - MemRegion: 轻量级范围描述
  - ReservedSpace: 虚拟地址管理
  - G1RegionToSpaceMapper: 物理内存管理

---

## B. 创建 6 个 G1RegionToSpaceMapper ✅ 已完成

> 详见 [B-Six-Mappers.md](B-Six-Mappers.md)

| 映射器 | 大小 | 映射比例 | 用途 |
|--------|------|----------|------|
| heap_storage | 8GB | 1:1 | Java 堆 |
| bot_storage | 16MB | 512B→1B | 块偏移表 |
| cardtable_storage | 16MB | 512B→1B | 卡表 |
| card_counts_storage | 16MB | 512B→1B | 热卡计数 |
| prev_bitmap_storage | 128MB | 64B→1bit | 上轮标记 |
| next_bitmap_storage | 128MB | 64B→1bit | 本轮标记 |

- [x] **B.1** heap_storage - create_mapper() 直接创建
- [x] **B.2** BOT - 快速定位对象起始
- [x] **B.3** CardTable - 跨 Region 引用跟踪
- [x] **B.4** CardCounts - 热卡优化
- [x] **B.5** 双缓冲位图 - 并发标记 + Mixed GC 同时进行

---

## C. 初始化核心数据结构

### C.1 HeapRegionManager 初始化 ✅ 已完成
```cpp
_hrm.initialize(heap_storage, prev_bitmap_storage, next_bitmap_storage,
                bot_storage, cardtable_storage, card_counts_storage);
```
- [x] **C.1.1** G1HeapRegionTable - 偏置数组实现 O(1) 地址查找
- [x] **C.1.2** FreeRegionList - 双向链表管理空闲 Region
- [x] **C.1.3** _available_map - 位图跟踪 Region 可用性

> 详见 [C.1-HeapRegionManager.md](C.1-HeapRegionManager.md)

### C.2 G1CardTable 初始化
```cpp
G1CardTable* ct = new G1CardTable(reserved_region());
ct->initialize();
_card_table->initialize(cardtable_storage);
```
- [x] **C.2.1** CardTable 的内存布局（已在 B 部分分析）
- [x] **C.2.2** 卡值含义：clean(-1)/dirty(0)/young(0x20)

### C.3 G1BarrierSet 创建 ✅ 已完成
```cpp
G1BarrierSet* bs = new G1BarrierSet(ct);
bs->initialize();
BarrierSet::set_barrier_set(bs);
```
- [x] **C.3.1** 写前屏障（SATB）- 并发标记防漏标
- [x] **C.3.2** 写后屏障（CardTable）- 跨代引用追踪

> 详见 [C.3-G1BarrierSet.md](C.3-G1BarrierSet.md)

### C.4 G1HotCardCache 初始化 ✅ 已完成
```cpp
_hot_card_cache = new G1HotCardCache(this);
_hot_card_cache->initialize(card_counts_storage);
```
- [x] **C.4.1** 热卡缓存的作用（延迟处理频繁修改的卡）
- [x] **C.4.2** 热卡判定阈值（修改 ≥4 次，G1ConcRSHotCardLimit）

> 详见 [C.4-G1HotCardCache.md](C.4-G1HotCardCache.md)

### C.5 G1RemSet 创建 ✅ 已完成
```cpp
_g1_rem_set = new G1RemSet(this, _card_table, _hot_card_cache);
_g1_rem_set->initialize(max_capacity(), max_regions());
```
- [x] **C.5.1** G1RemSetScanState - GC 期间多线程扫描协调
- [x] **C.5.2** G1FromCardCache - 卡片去重缓存

> 详见 [C.5-G1RemSet.md](C.5-G1RemSet.md)

### C.6 G1BlockOffsetTable 创建 ✅ 已完成
```cpp
_bot = new G1BlockOffsetTable(reserved_region(), bot_storage);
```
- [x] **C.6.1** BOT 的作用：O(log n) 查找对象起始地址
- [x] **C.6.2** 对数编码：支持大对象，保持 1 字节条目

> 详见 [C.6-G1BlockOffsetTable.md](C.6-G1BlockOffsetTable.md)

### C.7 快速测试数组初始化 ✅ 已完成
```cpp
_in_cset_fast_test.initialize(start, end, granularity);
_humongous_reclaim_candidates.initialize(start, end, granularity);
```
- [x] **C.7.1** _in_cset_fast_test：O(1) 判断 Region 是否在 CSet
- [x] **C.7.2** _humongous_reclaim_candidates：巨型对象快速回收候选
- [x] **C.7.3** InCSetState 编码设计：Humongous(-1)/NotInCSet(0)/Young(1)/Old(2)
- [x] **C.7.4** G1BiasedMappedArray 偏置数组原理：预计算偏移量实现无减法访问

> 详见 [C.7-FastTestArrays.md](C.7-FastTestArrays.md)

---

## D. 并发组件创建

### D.1 G1ConcurrentMark 创建 ✅ 已完成
```cpp
_cm = new G1ConcurrentMark(this, prev_bitmap_storage, next_bitmap_storage);
_cm_thread = _cm->cm_thread();
```
- [x] **D.1.1** G1ConcurrentMark 的职责
  - 双缓冲位图管理
  - 任务队列集合（13 个，每个 1MB）
  - 全局标记栈
- [x] **D.1.2** G1ConcurrentMarkThread 的创建
- [x] **D.1.3** 并发标记的五个阶段

> 详见 [D.1-G1ConcurrentMark.md](D.1-G1ConcurrentMark.md)

### D.2 G1ConcurrentRefine 创建 ✅ 已完成
```cpp
jint ecode = initialize_concurrent_refinement();
// _cr = G1ConcurrentRefine::create(&ecode);
```
- [x] **D.2.1** 并发精炼线程的创建（初始仅 1 个，动态扩展至 13 个）
- [x] **D.2.2** Green/Yellow/Red 三区域控制（13/39/65）

> 详见 [D.2-G1ConcurrentRefine.md](D.2-G1ConcurrentRefine.md)

### D.3 G1YoungRemSetSamplingThread 创建 ✅ 已完成
```cpp
ecode = initialize_young_gen_sampling_thread();
// _young_gen_sampling_thread = new G1YoungRemSetSamplingThread();
```
- [x] **D.3.1** RSet 采样的目的（动态调整年轻代长度）
- [x] **D.3.2** 每 300ms 采样一次（G1ConcRefinementServiceIntervalMillis）

> 详见 [D.3-G1YoungRemSetSamplingThread.md](D.3-G1YoungRemSetSamplingThread.md)

---

## E. 扩展堆（实际提交内存）【超级核心】

### E.1 expand() ✅ 已完成
```cpp
if (!expand(init_byte_size, _workers)) {
    vm_shutdown_during_initialization("Failed to allocate initial heap.");
}
```
- [x] **E.1.1** expand() 做了什么？
  - 预留 → 提交（PROT_NONE → PROT_READ|WRITE）
  - 创建 HeapRegion 对象
  - 提交辅助结构（BOT/CardTable/位图）
- [x] **E.1.2** HeapRegion 对象的创建
- [x] **E.1.3** Region 加入 FreeRegionList

> 详见 [E.1-expand.md](E.1-expand.md)

---

## F. 运行时组件初始化 ✅ 已完成

> 详见 [F-RuntimeComponents.md](F-RuntimeComponents.md)

### F.1 G1Policy 初始化 ✅
```cpp
g1_policy()->init(this, &_collection_set);
```
- [x] **F.1.1** 年轻代大小边界（min=5%=102, max=60%=1228）
- [x] **F.1.2** 自适应 vs 固定模式
- [x] **F.1.3** CSet 增量构建启动

### F.2 SATB 队列集合初始化 ✅
```cpp
G1BarrierSet::satb_mark_queue_set().initialize(
    SATB_Q_CBL_mon, SATB_Q_FL_lock,
    G1SATBProcessCompletedThreshold,  // 20
    Shared_SATB_Q_lock);
```
- [x] **F.2.1** SATB 队列架构（线程本地 + 共享队列）
- [x] **F.2.2** 缓冲区管理（完成队列阈值=20）

### F.3 脏卡队列集合初始化 ✅
```cpp
G1BarrierSet::dirty_card_queue_set().initialize(
    ..., 
    concurrent_refine()->yellow_zone(),  // 39
    concurrent_refine()->red_zone(),     // 65
    ...);
```
- [x] **F.3.1** 两个脏卡队列集合的区别
  - `G1BarrierSet` 的：触发精炼（Yellow=39, Red=65）
  - `G1CollectedHeap` 的：永不触发，借用空闲池
- [x] **F.3.2** Yellow/Red 阈值与精炼线程唤醒

### F.4 分配区域初始化 ✅
```cpp
HeapRegion* dummy_region = _hrm.get_dummy_region();
dummy_region->set_eden();
dummy_region->set_top(dummy_region->end());
G1AllocRegion::setup(this, dummy_region);
_allocator->init_mutator_alloc_region();
```
- [x] **F.4.1** Dummy Region 机制（空指针的优雅替代）
- [x] **F.4.2** G1Allocator 三种分配区域

### F.5 监控与去重 ✅
```cpp
_g1mm = new G1MonitoringSupport(this);
G1StringDedup::initialize();
```
- [x] **F.5.1** G1MonitoringSupport（jstat/JMX/日志）
- [x] **F.5.2** 字符串去重机制（候选队列 + 哈希表 + 后台线程）

### F.6 PreservedMarksSet 初始化 ✅
```cpp
_preserved_marks_set.init(ParallelGCThreads);
```
- [x] **F.6.1** Evacuation Failure 时保存 mark word
- [x] **F.6.2** 每个 GC Worker 一个栈（13个），避免锁竞争

---

## 内存布局总览（8GB 堆）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          虚拟地址空间布局                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Java 堆（8GB）                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ Region 0 │ Region 1 │ Region 2 │ ... │ Region 2047 │                   │ │
│  │   4MB    │   4MB    │   4MB    │     │    4MB      │                   │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  辅助数据结构（独立地址空间）                                                │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐                │
│  │ BOT (16MB)     │  │ CardTable(16MB)│  │ CardCounts(16MB│                │
│  │ 每 512B → 1B   │  │ 每 512B → 1B   │  │ 每 512B → 1B   │                │
│  └────────────────┘  └────────────────┘  └────────────────┘                │
│                                                                              │
│  ┌────────────────────────────────┐  ┌────────────────────────────────┐    │
│  │ Prev Bitmap (128MB)            │  │ Next Bitmap (128MB)            │    │
│  │ 每 64B → 1 bit                 │  │ 每 64B → 1 bit                 │    │
│  └────────────────────────────────┘  └────────────────────────────────┘    │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  辅助结构总计：16 + 16 + 16 + 128 + 128 = 304 MB                            │
│  堆总计：8GB + 304MB ≈ 8.3GB                                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 学习路径建议

### 推荐顺序
1. **E.1** expand() - 理解内存如何从预留到提交
2. **B 部分** - 理解 6 个映射器的作用
3. **C.1** HeapRegionManager - Region 如何被管理
4. **D.1** G1ConcurrentMark - 并发标记的核心

### 可跳过的部分
- A.1.3 大页处理（生产环境通常不用）
- F.5.2 字符串去重（可选特性）

---

## 如何使用这个子大纲

对我说：
- "分析 E.1 - expand() 堆扩展"
- "深入 B.5 - 并发标记位图双缓冲"
- "讲解 C.3 - G1BarrierSet 写屏障"

我会针对那个小节进行深入分析，包括：
- 完整源码展示
- 逐行注释
- GDB 验证命令和结果
- 设计考量和权衡
