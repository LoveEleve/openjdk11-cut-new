# 3.1 create_heap() 详细子大纲

> 这是 3.1 的递归细化大纲，每个小节都可以单独深入分析
> 标记说明：[ ] 待分析  [~] 部分分析  [x] 已完成

---

## 整体调用链

```
Universe::create_heap()                              [universe.cpp:876]
└── GCConfig::arguments()->create_heap()             [gcConfig.cpp:247]
    │
    │  // GCConfig::_arguments 指向 G1Arguments（静态对象）
    │
    └── G1Arguments::create_heap()                   [g1Arguments.cpp:178]
        └── create_heap_with_policy<G1CollectedHeap, G1CollectorPolicy>()
            ├── ① new G1CollectorPolicy()           [g1CollectorPolicy.cpp:35]
            │       └── HeapRegion::setup_heap_region_size()
            │       └── HeapRegionRemSet::setup_remset_size()
            ├── ② policy->initialize_all()
            │       └── initialize_alignments()
            └── ③ new G1CollectedHeap(policy)       [g1CollectedHeap.cpp:1444]
                    ├── 初始化列表（40+ 字段）
                    └── 构造函数体（创建子组件）
```

---

## A. GC 选择机制

### A.1 GCConfig::select_gc() - GC 选择入口
```cpp
// gcConfig.cpp:152
GCArguments* GCConfig::select_gc() {
  fail_if_unsupported_gc_is_selected();  // 检查不支持的 GC
  if (is_no_gc_selected()) {
    select_gc_ergonomically();           // 自动选择
  }
  // 返回选中的 GCArguments
}
```
- [ ] **A.1.1** `SupportedGC` 结构体详解
  - 字段：`_flag`（如 UseG1GC）、`_name`（枚举）、`_arguments`（GCArguments&）、`_hs_err_name`
  - 为什么 `_arguments` 是引用而不是指针？
  - 静态对象 `g1Arguments` 何时初始化？（C++ 静态初始化顺序）

- [ ] **A.1.2** `SupportedGCs` 静态表的设计
  - 7 种 GC 的映射关系
  - `FOR_EACH_SUPPORTED_GC` 宏的展开
  - 条件编译宏：`G1GC_ONLY_ARG`、`SERIALGC_ONLY_ARG` 等

- [ ] **A.1.3** 为什么不支持同时选择多个 GC？
  - `is_exactly_one_gc_selected()` 的实现
  - 错误信息："Multiple garbage collectors selected"

### A.2 自动选择逻辑
```cpp
// gcConfig.cpp:108
void GCConfig::select_gc_ergonomically() {
  if (os::is_server_class_machine()) {
    FLAG_SET_ERGO_IF_DEFAULT(bool, UseG1GC, true);
  } else {
    FLAG_SET_ERGO_IF_DEFAULT(bool, UseSerialGC, true);
  }
}
```
- [ ] **A.2.1** `is_server_class_machine()` 的判断标准
  - CPU ≥ 2 且内存 ≥ (2GB - 256MB)
  - 为什么要减去 256MB？（内核保留内存）
  - `os::active_processor_count()` vs `os::initial_active_processor_count()`

- [ ] **A.2.2** `FLAG_SET_ERGO_IF_DEFAULT` 宏详解
  - ERGO = Ergonomics（人机工程学/自动调优）
  - 只在用户未显式设置时才生效
  - Flag 的三种来源：DEFAULT、CMDLINE、ERGO

---

## B. G1Arguments::initialize() - 参数初始化

> 源码：`g1Arguments.cpp:90-175`

### B.1 ParallelGCThreads 计算 ✅
```cpp
FLAG_SET_DEFAULT(ParallelGCThreads, Abstract_VM_Version::parallel_worker_threads());
```
- [x] **B.1.1** `nof_parallel_worker_threads(5, 8, 8)` 算法详解 ✅
  - 公式：`ncpus ≤ 8 ? ncpus : 8 + (ncpus - 8) * 5/8`
  - 为什么大机器要打折？（收益递减、避免资源争用）
  - 图解：CPU 数量与线程数的关系曲线
  - **GDB 验证**：16 核机器 → ParallelGCThreads = 13 ✓

> 详细分析见：`jvm-md/Universe/B.1.1-ParallelGCThreads.md`

- [ ] **B.1.2** 我的机器（16 核）计算过程
  - `threads = 8 + (16 - 8) * 5/8 = 8 + 5 = 13` ✓

### B.2 G1ConcRefinementThreads
```cpp
FLAG_SET_ERGO(uint, G1ConcRefinementThreads, ParallelGCThreads);
```
- [ ] **B.2.1** 为什么默认等于 ParallelGCThreads？
- [ ] **B.2.2** 并发细化线程的作用（RSet 更新）

### B.3 MarkStackSizeMax
```cpp
FLAG_SET_DEFAULT(MarkStackSizeMax, 128 * TASKQUEUE_SIZE); // 131072
```
- [ ] **B.3.1** TASKQUEUE_SIZE 的定义（1024 个条目）
- [ ] **B.3.2** 并发标记栈的用途
- [ ] **B.3.3** 为什么是 128 倍？

### B.4 GCTimeRatio
```cpp
FLAG_SET_DEFAULT(GCTimeRatio, 12);  // GC 时间占比 ≈ 1/(1+12) ≈ 7.7%
```
- [ ] **B.4.1** GCTimeRatio 的含义
  - 公式：GC 时间占比 = 1 / (1 + GCTimeRatio)
  - 为什么 G1 默认是 12 而 PS 是 99？
- [ ] **B.4.2** 这个参数如何影响堆扩展策略？

### B.5 MaxGCPauseMillis【核心参数】
```cpp
FLAG_SET_DEFAULT(MaxGCPauseMillis, 200);  // 200ms 暂停时间目标
```
- [ ] **B.5.1** 这是 G1 最核心的调优参数
- [ ] **B.5.2** G1 如何根据这个目标调整年轻代大小？
- [ ] **B.5.3** 暂停时间预测模型（G1Predictions）

### B.6 GCPauseIntervalMillis
```cpp
FLAG_SET_DEFAULT(GCPauseIntervalMillis, MaxGCPauseMillis + 1);  // 201ms
```
- [ ] **B.6.1** 为什么是 +1？（给 G1 最大灵活性）
- [ ] **B.6.2** MMU (Minimum Mutator Utilization) 的概念

### B.7 ParallelRefProcEnabled
```cpp
FLAG_SET_DEFAULT(ParallelRefProcEnabled, true);  // 多线程时启用
```
- [ ] **B.7.1** 引用处理的四种类型（Soft/Weak/Final/Phantom）
- [ ] **B.7.2** 并行处理如何加速引用清理？

### B.8 Loop Strip Mining【C2 编译器相关】
```cpp
FLAG_SET_DEFAULT(UseCountedLoopSafepoints, true);
FLAG_SET_DEFAULT(LoopStripMiningIter, 1000);
```
- [ ] **B.8.1** 什么是 Loop Strip Mining？
- [ ] **B.8.2** 为什么长循环会阻塞 GC？
- [ ] **B.8.3** 每 1000 次迭代插入安全点检查

---

## C. G1CollectorPolicy 构造函数

> 源码：`g1CollectorPolicy.cpp:35-50`

### C.1 HeapRegion::setup_heap_region_size() ✅
```cpp
HeapRegion::setup_heap_region_size(InitialHeapSize, MaxHeapSize);
```
- [x] **C.1.1** Region 大小计算算法 → 详见 [C.1.1-RegionSize.md](C.1.1-RegionSize.md)
  - 公式：`region_size = MAX(avg_heap/2048, 1MB)` → 向下取整为 2 的幂 → clamp(1MB, 32MB)
  - 8GB 堆 → 4MB Region → 2048 个 Region
  - **GDB 验证**：GrainBytes=4MB, LogOfHRGrainBytes=22, CardsPerRegion=8192 ✓
- [x] **C.1.2** 为什么选择 2048 个 Region？
  - 平衡管理开销和回收粒度
  - 11 位索引，位运算高效
- [x] **C.1.3** 设置的静态变量
  - `GrainBytes` = 4MB
  - `GrainWords` = 524288
  - `LogOfHRGrainBytes` = 22
  - `CardsPerRegion` = 8192

### C.2 HeapRegionRemSet::setup_remset_size() ✅
- [x] **C.2.1** RemSet 大小的计算 → 详见 [C.2-RemSetSize.md](C.2-RemSetSize.md)
  - 公式：`entries = base × (region_size_log_mb + 1)`
  - 8GB 堆：G1RSetSparseRegionEntries=12, G1RSetRegionEntries=768
- [x] **C.2.2** 稀疏/细粒度/粗粒度 PRT 三层结构
  - Sparse：每源 Region 最多 12 张卡
  - Fine：最多 768 个 PerRegionTable（每个 1KB Bitmap）
  - Coarse：2048 位 Bitmap（256 字节）

### C.3 initialize_alignments() ✅
```cpp
_space_alignment = HeapRegion::GrainBytes;
_heap_alignment = MAX3(card_table_alignment, _space_alignment, page_size);
```
- [x] **C.3.1** 三种对齐的含义 → 详见 [C.3-Alignments.md](C.3-Alignments.md)
  - `_space_alignment`：Region 对齐（4MB）
  - `_heap_alignment`：堆对齐（4MB）
  - `card_table_alignment`：卡表对齐（2MB = 512 × 4K）
- [x] **C.3.2** 为什么需要对齐？
  - 性能优化 + 位运算优化 + OS 约束

---

## D. G1CollectedHeap 构造函数 - 初始化列表

> 源码：`g1CollectedHeap.cpp:1444-1497`
> 约 40+ 个字段的初始化

### D.1 策略与线程相关 ✅
```cpp
_young_gen_sampling_thread(NULL),
_collector_policy(collector_policy),
_soft_ref_policy(),
```
- [x] **D.1.1** `_young_gen_sampling_thread` 的作用 → 详见 [D.1-D.3-PolicyTimerTracer.md](D.1-D.3-PolicyTimerTracer.md)
  - 周期采样 RSet，预测暂停时间
  - 每 300ms 采样一次（G1ConcRefinementServiceIntervalMillis）
- [x] **D.1.2** `_soft_ref_policy` 软引用清理策略
  - `SoftRefLRUPolicyMSPerMB=1000`：每 MB 空闲保留 1 秒
  - **生产实践**：8GB 堆空闲 4GB → 保留 66 分钟 ✓

### D.2 JMX 监控相关 ✅
```cpp
_memory_manager("G1 Young Generation", "end of minor GC"),
_full_gc_memory_manager("G1 Old Generation", "end of major GC"),
_eden_pool(NULL),
_survivor_pool(NULL),
_old_pool(NULL),
```
- [x] **D.2.1** `GCMemoryManager` 的作用
  - JMX MemoryManagerMXBean 实现
  - 记录 GC 次数和累积时间
- [x] **D.2.2** 三个内存池（Eden/Survivor/Old）
  - 在 `initialize()` 中创建
  - **生产实践**：jstat/JMX 监控 ✓

### D.3 GC 计时与追踪 ✅
```cpp
_gc_timer_stw(new(ResourceObj::C_HEAP, mtGC) STWGCTimer()),
_gc_tracer_stw(new(ResourceObj::C_HEAP, mtGC) G1NewTracer()),
```
- [x] **D.3.1** `STWGCTimer` 的实现
  - 记录各阶段耗时（Ext Root、Update RS、Object Copy 等）
- [x] **D.3.2** `G1NewTracer` 与 JFR 的关系
  - 生成 JFR 事件：jdk.G1GarbageCollection、jdk.G1HeapSummary
- [x] **D.3.3** 内存分配方式 `new(ResourceObj::C_HEAP, mtGC)`
  - C_HEAP：分配在 C 堆（NMT 可追踪）
  - mtGC：内存类型标记为 GC

### D.4 G1 核心组件
```cpp
_g1_policy(new G1Policy(_gc_timer_stw)),
_collection_set(this, _g1_policy),
```
- [x] **D.4.1** `G1Policy` 构造函数详解【重点】→ 详见 [D.4.1-G1Policy.md](D.4.1-G1Policy.md)
  - 预测器 `_predictor`（衰减平均）
  - 分析器 `_analytics`（历史数据）
  - MMU 追踪器 `_mmu_tracker`
  - IHOP 控制器 `_ihop_control`
  - **GDB 验证**：sigma=0.5, time_slice=0.201s, max_gc_time=0.200s ✓
- [x] **D.4.2** `G1CollectionSet` 的初始化 → 详见 [D.4.2-G1CollectionSet.md](D.4.2-G1CollectionSet.md)
  - 收集集增量构建机制
  - CollectionSetChooser 老年代选择策略
  - 暂停时间目标与预算分配
  - **GDB 验证**：_collection_set_mem_id=3, inc_build_state=Inactive ✓

### D.5 脏卡队列 ✅
```cpp
_dirty_card_queue_set(false),  // false 表示不是 SATB 队列
```
- [x] **D.5.1** DirtyCardQueueSet vs SATBMarkQueueSet → 详见 [D.5-D.6-QueuesAndRefProc.md](D.5-D.6-QueuesAndRefProc.md)
  - DirtyCardQueue：写后屏障，记录脏卡，维护 RSet
  - SATBMarkQueue：写前屏障，仅并发标记期间，记录旧引用
- [x] **D.5.2** `false` 参数的含义
  - notify_when_complete 参数
  - **生产实践**：监控 Update RS 时间、精炼线程调优 ✓

### D.6 引用处理器 ✅
```cpp
// STW 阶段
_ref_processor_stw(NULL),
_is_alive_closure_stw(this),
_is_subject_to_discovery_stw(this),
// 并发标记阶段
_ref_processor_cm(NULL),
_is_alive_closure_cm(this),
_is_subject_to_discovery_cm(this),
```
- [x] **D.6.1** 为什么有两套引用处理器？
  - STW：Young/Mixed GC 期间，CSet 范围
  - CM：并发标记期间，整堆范围
- [x] **D.6.2** `_is_alive_closure` 闭包的实现
  - STW：对象在 CSet 外或已复制
  - CM：对象被并发标记标记为存活
- [x] **D.6.3** `_is_subject_to_discovery` 的作用
  - 决定哪些区域的引用需要被发现
  - **生产实践**：`-Xlog:gc+ref*=debug` 监控引用处理 ✓

### D.7 辅助数据结构（NULL 初始化）✅
```cpp
_bot(NULL),            // Block Offset Table
_hot_card_cache(NULL), // 热卡缓存
_g1_rem_set(NULL),     // Remembered Set
_cr(NULL),             // Concurrent Refinement
_g1mm(NULL),           // Monitoring Support
```
- [x] **D.7.1** 为什么这些字段先设为 NULL？ → 详见 [D.7-AuxiliaryStructures.md](D.7-AuxiliaryStructures.md)
  - 需要等堆内存分配完成后才能创建
  - 在 `initialize()` 中实际分配
- [x] **D.7.2** 每个字段的用途概述
  - `_bot`：BOT 对象起始查找，16MB（8GB 堆）
  - `_hot_card_cache`：热卡缓存，延迟写屏障处理，1024 条目
  - `_g1_rem_set`：RSet 管理器，三层结构
  - `_cr`：并发精炼，Green/Yellow/Red 三区域控制
  - `_g1mm`：JMX/jstat 监控支持
  - **生产实践**：RSet 膨胀诊断、精炼线程调优、监控指标 ✓

### D.8 Region 集合 ✅
```cpp
_old_set("Old Set", false, new OldRegionSetMtSafeChecker()),
_humongous_set("Master Humongous Set", true, new HumongousRegionSetMtSafeChecker()),
```
- [x] **D.8.1** `HeapRegionSet` 的数据结构 → 详见 [D.8-D.10-InitListFields.md](D.8-D.10-InitListFields.md)
  - 计数器实现（非链表）
  - FreeRegionList 才是双向链表
- [x] **D.8.2** `MtSafeChecker` 的作用
  - 检查是否在安全点或持有堆锁
  - DEBUG 构建中断言检查

### D.9 统计与 PLAB ✅
```cpp
_summary_bytes_used(0),
_survivor_evac_stats("Young", YoungPLABSize, PLABWeight),
_old_evac_stats("Old", OldPLABSize, PLABWeight),
```
- [x] **D.9.1** PLAB (Promotion Local Allocation Buffer) 机制
  - GC 线程本地分配缓冲，减少竞争
  - YoungPLABSize=32KB, OldPLABSize=8KB
- [x] **D.9.2** PLABWeight 参数的作用
  - 自适应调整权重（默认 75）
  - **生产实践**：监控 `-Xlog:gc+plab=debug` ✓

### D.10 其他标志 ✅
```cpp
_expand_heap_after_alloc_failure(true),
_old_marking_cycles_started(0),
_old_marking_cycles_completed(0),
_in_cset_fast_test(),
```
- [x] **D.10.1** `_expand_heap_after_alloc_failure` 的策略
  - Xms=Xmx 时实际无法扩展
- [x] **D.10.2** `_in_cset_fast_test` 的位图实现【重要】
  - O(1) 判断对象是否在 CSet
  - 4 种状态：NotInCSet(0), Young(1), Old(2), Humongous(-1)
  - 8GB 堆 = 2048 字节偏置数组 ✓

---

## E. G1CollectedHeap 构造函数 - 函数体

> 源码：`g1CollectedHeap.cpp:1499-1536`

### E.1 WorkGang 创建 ✅
```cpp
_workers = new WorkGang("GC Thread", ParallelGCThreads,
                        /* are_GC_task_threads */true,
                        /* are_ConcurrentGC_threads */false);
_workers->initialize_workers();
```
- [x] **E.1.1** WorkGang 类的设计 → 详见 [E.1-WorkGang.md](E.1-WorkGang.md)
  - 继承关系：AbstractWorkGang → WorkGang
  - 与 Java ThreadPoolExecutor 对比
- [x] **E.1.2** `are_GC_task_threads` 参数的含义
  - true = STW 并行 GC 线程（pgc_thread）
- [x] **E.1.3** `initialize_workers()` 做了什么？
  - 分配指针数组（13 × 8 = 104 字节）
  - 动态模式下只创建 1 个 GangWorker
- [x] **E.1.4** 线程此时是否启动？
  - 是！`os::start_thread()` 立即启动
  - **GDB 验证**：_total_workers=13, _created_workers=1 ✓

### E.2 验证器和分配器 ✅
```cpp
_verifier = new G1HeapVerifier(this);
_allocator = new G1Allocator(this);
```
- [x] **E.2.1** `G1HeapVerifier` 的验证内容 → 详见 [E.2-VerifierAllocator.md](E.2-VerifierAllocator.md)
  - 堆一致性检查（6 种验证类型）
  - `-XX:+VerifyBeforeGC` / `-XX:+VerifyAfterGC`
- [x] **E.2.2** `G1Allocator` 的职责
  - 三个分配区域：Mutator（应用线程）、Survivor（GC 存活）、Old（晋升）
  - BOT 更新差异：Survivor=false, Old=true
  - **GDB 验证**：_g1h 指向堆，三个区域已初始化 ✓

### E.3 堆大小调整策略 ⏭️ (跳过)
```cpp
_heap_sizing_policy = G1HeapSizingPolicy::create(this, _g1_policy->analytics());
```
- [~] **E.3.1** `G1HeapSizingPolicy` 的决策逻辑
  - **跳过原因**：生产环境标准配置 `-Xms = -Xmx`，堆大小固定
  - 此时 `uncommitted_bytes = 0`，扩展逻辑永远返回 0
  - 只有 `Xms < Xmx` 时才有意义（开发/测试环境）
- [~] **E.3.2** 核心逻辑简述（供参考）
  - 当 GC 开销 > `1/(1+GCTimeRatio)` 且连续 4 次超标时触发扩展
  - 扩展量根据超标幅度动态调整（0.2x ~ 2x 缩放因子）

### E.4 巨型对象阈值 ✅
```cpp
_humongous_object_threshold_in_words = humongous_threshold_for(HeapRegion::GrainWords);
// 8GB 堆：4MB Region / 2 = 2MB
```
- [x] **E.4.1** `humongous_threshold_for()` 的计算 → 详见 [E.4-HumongousThreshold.md](E.4-HumongousThreshold.md)
  - 公式：Region 大小的一半
  - 8GB 堆：524,288 / 2 = 262,144 words = 2MB
  - **GDB 验证**：threshold=262144 words (2MB) ✓
- [x] **E.4.2** 巨型对象的分配方式
  - 连续 Region 分配（StartsHumongous + ContinuesHumongous）
  - 直接进入老年代

### E.5 任务队列
```cpp
uint n_queues = ParallelGCThreads;
_task_queues = new RefToScanQueueSet(n_queues);
_evacuation_failed_info_array = NEW_C_HEAP_ARRAY(EvacuationFailedInfo, n_queues, mtGC);

for (uint i = 0; i < n_queues; i++) {
    RefToScanQueue* q = new RefToScanQueue();
    q->initialize();
    _task_queues->register_queue(i, q);
    ::new(&_evacuation_failed_info_array[i]) EvacuationFailedInfo();
}
```
- [x] **E.5.1** `RefToScanQueue` 的数据结构 → 详见 [E.5.1-RefToScanQueue.md](E.5.1-RefToScanQueue.md)
  - ABP 工作窃取队列（Work-Stealing Queue）
  - 13 个 GC 线程如何协作
- [x] **E.5.2** 工作窃取的负载均衡 → 详见 [E.5.2-E.5.3-StealAndEvacFailed.md](E.5.2-E.5.3-StealAndEvacFailed.md)
  - `steal_best_of_2()`：随机二选一算法
  - `ParallelTaskTerminator`：终止协议（自旋→yield→sleep）
  - **生产实践**：监控 Termination 时间、调优参数 ✓
- [x] **E.5.3** `EvacuationFailedInfo` 的作用 → 详见 [E.5.2-E.5.3-StealAndEvacFailed.md](E.5.2-E.5.3-StealAndEvacFailed.md)
  - 每线程一个实例，无锁统计
  - 记录：first_size, smallest_size, total_size, count
  - GC 结束后汇总报告 JFR 事件
  - **生产实践**：To-space exhausted 日志、G1ReservePercent 预防 ✓

### E.6 GC 追踪器初始化 ✅
```cpp
_gc_tracer_stw->initialize();
```
- [x] **E.6.1** JFR 事件注册 → 详见 [E.6-GCTracerInit.md](E.6-GCTracerInit.md)
  - `register_jfr_type_constants()`：注册 G1 特有类型常量
  - `G1HeapRegionTypeConstant`：9 种 Region 类型（Free/Eden/Survivor/Old/...）
  - `G1YCTypeConstant`：4 种 YC 类型（Normal/InitialMark/DuringMark/Mixed）
- [x] **E.6.2** 性能计数器初始化
  - 通过 JFR 事件实现：EventG1GarbageCollection、EventEvacuationInformation
  - 与 STWGCTimer 配合：Timer 记录时间 → Tracer 报告 JFR
  - **生产实践**：`-XX:StartFlightRecording` 启用 JFR 监控 ✓

---

## F. G1Policy 构造函数【决策核心】

> 源码：`g1Policy.cpp:49-71`

### F.1 预测器 G1Predictions ✅
```cpp
_predictor(G1ConfidencePercent / 100.0),  // 默认 50%
```
- [x] **F.1.1** 衰减平均算法 ✅
  - 公式：`prediction = davg + sigma * stddev_estimate`
  - `davg_new = (1-α) × 新值 + α × davg_old`，α=0.7
- [x] **F.1.2** `stddev_estimate()` 的特殊处理 ✅
  - 样本数 < 5 时的保守估计
- [x] **F.1.3** G1ConfidencePercent 参数的影响 ✅
  - 值越大，预测越保守
  - **GDB 验证**：sigma = 0.5, cost_per_card_ms = 0.0015ms ✓

> 详细分析见：`jvm-md/Universe/F.1-G1Predictions.md`

### F.2 分析器 G1Analytics ✅
```cpp
_analytics(new G1Analytics(&_predictor)),
```
- [x] **F.2.1** 15+ 个 TruncatedSeq 序列 → 详见 [F.2-G1Analytics.md](F.2-G1Analytics.md)
  - `_recent_gc_times_ms`：最近 GC 耗时
  - `_alloc_rate_ms_seq`：分配速率
  - `_cost_per_card_ms_seq`：每张卡的处理成本
  - ... 等等
- [x] **F.2.2** 默认值表（按 ParallelGCThreads 分级）
  - `cost_per_card_ms_defaults[]`
  - `cost_per_entry_ms_defaults[]`
  - **GDB 验证**：index=7, cost_per_card=0.0015ms ✓
- [x] **F.2.3** TruncatedSeq 的实现
  - 固定长度的滑动窗口（10）
  - 自动计算衰减平均值和标准差

### F.3 MMU 追踪器 ✅
```cpp
_mmu_tracker(new G1MMUTrackerQueue(GCPauseIntervalMillis / 1000.0, MaxGCPauseMillis / 1000.0)),
```
- [x] **F.3.1** MMU (Minimum Mutator Utilization) 的定义 → 详见 [F.3-G1MMUTracker.md](F.3-G1MMUTracker.md)
  - 在给定时间窗口内，应用程序能运行的最小比例
  - MMU = 1 - (200/201) ≈ 0.5%
- [x] **F.3.2** 时间窗口和暂停目标的关系
  - **GDB 验证**：_time_slice=0.201s, _max_gc_time=0.200s ✓

### F.4 IHOP 控制器 ✅
```cpp
_ihop_control(create_ihop_control(&_old_gen_alloc_tracker, &_predictor)),
```
- [x] **F.4.1** IHOP = Initiating Heap Occupancy Percent → 详见 [F.4-G1IHOPControl.md](F.4-G1IHOPControl.md)
  - 何时启动并发标记？
- [x] **F.4.2** 自适应 IHOP 算法
  - threshold = actual_target - predicted_needed
  - **GDB 验证**：G1UseAdaptiveIHOP=true, InitiatingHeapOccupancyPercent=45 ✓
- [x] **F.4.3** `-XX:InitiatingHeapOccupancyPercent` 参数

### F.5 存活率统计 ✅
```cpp
_short_lived_surv_rate_group(new SurvRateGroup()),
_survivor_surv_rate_group(new SurvRateGroup()),
```
- [x] **F.5.1** SurvRateGroup 的数据结构 → 详见 [F.5-SurvRateGroup.md](F.5-SurvRateGroup.md)
  - 按 age 统计存活率
  - 使用 TruncatedSeq（窗口=10）记录历史
  - 初始假设存活率：**40%**
- [x] **F.5.2** 如何影响晋升决策？
  - 累积存活率预测复制字节数
  - 影响年轻代大小调整

---

## G. GDB 验证检查点

- [ ] **G.1** create_heap() 入口断点验证
  - 断点：`universe.cpp:926`
  - 检查：GC 选择结果

- [ ] **G.2** G1CollectorPolicy 构造后验证
  - 断点：`g1Arguments.cpp:179`
  - 检查：HeapRegion 静态变量

- [ ] **G.3** G1CollectedHeap 构造后验证
  - 断点：`universe.cpp:928`
  - 检查：所有字段初始值

- [ ] **G.4** G1Policy 字段验证
  - 预测器参数
  - 分析器默认值

---

## 如何使用这个子大纲

对我说：
- "分析 A.2.1 - is_server_class_machine()"
- "深入 B.1.1 - ParallelGCThreads 计算算法"
- "讲解 D.4.1 - G1Policy 构造函数"
- "展开 F.1 - G1Predictions 预测器"

我会针对那个小节进行深入分析，包括：
- 完整源码展示
- 逐行注释
- GDB 验证命令和结果
- 设计考量和权衡
